# 模型合并与编辑：局部修改、组合与重载

<!-- learning-contract -->
<div class="learning-contract" markdown="1">

**学习导航**

- **适合读者**：需要局部修改模型行为，或组合多个 checkpoint/Adapter 的训练与部署工程师。
- **先修**：[持续学习](continual-learning.md)、[微调与 PEFT](finetuning.md)和 checkpoint 身份概念。
- **首次阅读**：模型编辑 → 线性插值与 task arithmetic → LoRA 合并 → 独立重载与回归。
- **完成信号**：能为一次编辑或合并写出兼容前提、对照候选、局部性测试和重载验收。
- **卡住时**：先比较 base、单任务工件和一个简单插值，不要同时改变量化或运行时。

</div>

持续学习回答“怎样沿时间学习并保留旧能力”。本页处理另一类动作：直接修改少量行为，或把已经产生的
权重/Adapter 组合成新工件。它们都可能产生远离目标样例的干扰，因此不能用一个成功例或一次内存内前向作为验收。

## 模型编辑：改一个事实，别顺手改坏邻居

模型编辑试图只修改少量事实或行为。无论使用梯度、闭式解、低秩更新还是学习得到的编辑器，都要检查：

- **目标生效**：目标输入是否按预期改变；
- **表达泛化**：改写、多语言问法是否同步改变；
- **相关推论**：依赖该事实的结论是否一致；
- **局部性**：无关事实与相似实体是否保持；
- **持久性**：保存、量化或继续训练后是否仍然生效；
- **冲突处理**：RAG、旧参数与新编辑冲突时采用哪一个。

单个 prompt 改对不代表知识全局一致。若事实经常变化或必须引用来源，外部知识存储通常更可控。

## 权重合并不是把能力做加法

假设团队分别为 Task A 和 Task B 训练了 checkpoint 或 adapter，现在想合成一个发布工件。
合并解决的是“怎样组合参数”，不会自动解决遗忘、数据冲突或任务选择。

### 线性插值

对共享架构的两个 checkpoint：

\[
\theta_{merge}=(1-\alpha)\theta_A+\alpha\theta_B.
\]

只有当两组权重处于兼容的表示区域和坐标系时，插值才可能工作。
相同架构但独立初始化的神经网络存在隐藏单元置换等对称性，逐坐标平均没有天然的语义对齐保证。

### Task arithmetic

若多个微调模型都从同一底座 \(\theta_0\) 出发，可以定义任务向量（task vector）：

\[
\Delta_i=\theta_i-\theta_0,
\qquad
\theta_{merge}=\theta_0+\sum_i\lambda_i\Delta_i.
\]

共享初始化提高了坐标兼容性，但 task vector 仍可能在同一参数上方向相反或幅度冲突。
线性相加不是“能力也会线性相加”的定理，仍要调节系数并执行完整评测。

### LoRA 合并

LoRA update 为 \(\Delta W=sBA\)。把多个 adapter 的 dense updates 相加可得到

\[
W'=W+\sum_i\lambda_i\Delta W_i,
\]

合并前要确认它们使用相同的 base revision、目标层、张量形状、矩阵方向和缩放约定。
若把多个更新重新压缩到固定 rank，需要 SVD 或其他近似，因此会产生误差。

多个 adapter 依次启用、并行相加，以及先合并再量化，也可能得到不同结果。

## 合并后要重新加载再验收

对每个合并候选至少比较：

- 每个单任务 checkpoint；
- base model；
- 简单插值或平均；
- 联合训练或多任务基线（若可行）；
- 不同合并系数与不同顺序。

保存合并工件后，用目标运行时重新加载，再测试每项能力、通用回归、安全、校准、生成长度和量化后表现。
几个成功样例无法暴露任务之间的破坏性干扰。

## 合并工件的身份与边界

每个输出至少绑定父模型 revision、tokenizer/模板、输入 checkpoint 或 Adapter、合并顺序与系数、dtype、
量化状态、代码版本和评测报告。回滚时恢复整组兼容组件，而不是只替换一个权重文件。

仓库的 LoRA 小实验能够比较合并前后的数值结果与独立重载，但不证明多个真实任务的能力可以相加，
也不覆盖目标量化运行时或生产质量。

## 自测与实践

1. 为什么同一 base 上的两个微调工件比独立初始化模型更适合 task arithmetic？
2. 写出两个 LoRA 可以相加或重压缩前必须一致的五项契约。
3. 为一次事实编辑设计目标、paraphrase、neighborhood、unrelated 与安全回归集。
4. 解释为什么保存后必须在新进程和目标运行时重新加载。
