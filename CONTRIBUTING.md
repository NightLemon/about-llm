# 贡献与写作规范

## 内容原则

- **准确**：事实与结论可追溯；区分论文结论、社区经验和作者判断。
- **教学友好**：从读者正在解决的问题出发；第一次出现的缩写必须展开，先解释直觉，再给公式和边界条件。
- **可复现**：实验写清模型、数据、软件版本、随机种子、硬件和评价指标。
- **不过时伪装**：产品价格、模型榜单、法律政策等必须标注查询日期。
- **尊重版权与许可**：不复制受限材料；代码、图片和数据注明来源与许可证。

## 文字要像人在讲解

学习契约和证据边界是页面接口，不是正文语气。写正文时遵守下面几条：

- 开头先给一个具体问题、现象或失败现场，让读者知道为什么要继续读。
- 一段只推进一个判断。需要用三个以上分号连接的句子，通常应该拆开并补上因果关系。
- 列表只用于步骤、选项和并列事实。需要解释“为什么”时，优先写成有主语和转折的段落。
- 中文负责讲清意思，英文术语放在第一次出现处。不要用一串 English nouns 代替解释。
- 先把机制讲完整，再集中说明它的适用边界。不要在每句话后面追加“但这不能证明……”。
- 少用“不是 X，而是 Y”“X 不等于 Y”“不要……”连续纠错。能直接说出 X 与 Y 的关系时，就直接说。
- 例子中的动作要有执行者，状态变化要有先后顺序。避免“进行验证”“完成处理”一类看不出谁做了什么的句子。
- 写完后朗读一遍。若一句话需要回头找主语，或读者必须同时记住四个抽象名词，继续改写。

### 把验证语言翻译成读者能理解的话

测试代码里的名字可以保持精确，正文却应该先说清楚它做了什么。下面这些改写不是词语替换，
而是在补回动作的执行者和原因：

| 测试报告式写法 | 教材写法 |
|---|---|
| 本实验拒绝浮动版本 | 为了让结果可以复现，本实验使用以下版本和参数 |
| strict verifier 拒绝 identity drift | 运行前，检查程序会核对版本；发现不同版本时会停止并指出差异 |
| authored fixture | 本仓库为解释这个机制准备的固定样例 |
| oracle 通过 | 手算结果与程序输出一致 |
| control 证明 | 对照实验观察到；随后具体写出观察到的状态 |
| LLM judge 通过 | 经校准的评审模型判断为通过；随后说明评分规则和人工参照 |
| frozen/held-out set | 一份未参与本轮选择、且内容版本固定的留出数据 |
| artifact verified | 具体说明哪份产物、哪些字段以及采用了什么校验 |

`revision`、`fingerprint`、`prefill` 等确有工程含义的词可以保留，但第一次出现时要解释它在当前例子中
对应什么。采用中文译名、保留英文还是标为讲解性表达，以[术语表](docs/reference/glossary.md)为准。
若读者必须先懂测试框架的内部命名才能理解一段教材，说明正文还没有写完。

页面不必机械复制相同标题。只要学习契约、主问题、例子、工程判断和自测都能找到，章节顺序应服从内容本身。

## 页面职责：同一信息只设一个权威入口

新增或扩写内容前，先判断它应由哪一类页面负责：

| 页面类型 | 唯一职责 | 不应承担 |
|---|---|---|
| 教学正文 | 解释一个主问题的直觉、机制、边界与第一次阅读路线 | 完整运行日志、项目安装手册或可复制治理表单 |
| Lab | 隔离一个变量，让读者运行、预测并观察最小机制 | 重讲整章背景或包装成生产方案 |
| 站点项目页 | 说明项目为何存在、学习顺序、验收层级与证据边界 | 复制命令参考和逐文件维护说明 |
| 项目 README | 提供该目录的安装、命令、输入输出、故障排查与开发接口 | 承担跨主题课程导航 |
| 参考页 | 供查找公式、术语、模板、来源规则或稳定接口 | 强迫读者按线性教程从头读完 |
| 证据页 | 保存版本、固定输入、原始结果、控制、失败和不可外推范围 | 用高密度台账替代教学解释 |

跨页面重复时，权威页保留完整解释，其他页面写一段足以做决定的摘要并链接过去。事实或实验结果搬到
证据页后，正文仍要紧邻保留结论边界；链接不能代替读者理解当前段落所需的最小解释。

需要盘点页面体量与职责漂移时，运行 `python scripts/report_content_structure.py --scope reader --format text`。
它按入口、正文、实践和 README 使用不同提示阈值；参考页免除篇幅提示，证据页只提示教程式职责漂移。
这些 finding 供审阅排序，不是 CI 失败条件；只有参数、配置或输入读取失败才返回非零状态。

## 章节起稿

新章节可从 `docs/_templates/topic.md` 起稿。模板提供检查点，不要求最终页面保留所有占位标题。通常需要：

1. 页面顶部的学习契约：适合读者、先修、首次阅读路径、完成信号和卡住时入口
2. 一个能让读者进入问题的具体场景
3. 机制、公式与贯穿始终的最小例子
4. 工程选择、权衡和失败模式
5. 自测题与延伸阅读

`models/`、`foundations/`、`core/`、`training/`、`systems/`、`applications/`、`quality/` 和 `frontier/` 下的每篇教学正文必须且只能包含一个 `<!-- learning-contract -->`。`python scripts/check_docs.py` 会验证标记、五个字段、链接和锚点。

同一检查还执行可读性棘轮：新教学页默认不得出现超过 200 字符的 prose 行，不得超过 45 个小节或 600 行。现有超标页记录在 `docs/reference/readability-baseline.json`；指标只能下降，改善正文时必须同步收紧对应 baseline。`docs/evidence/` 允许保存高密度台账，但开头必须把第一次阅读者引回教学入口。

## 状态标记

- `✅`：已有可独立学习的正文
- `🟡`：有骨架或简述，仍需扩充
- `⬜`：待编写

“完成”不表示主题永远不再更新，只表示已达到当前版本的最低教学标准。

## 引用

优先引用论文原文、官方文档、标准和可信的一手工程资料。论文用“作者，标题，会议/期刊或 arXiv，年份，链接”的格式。对存在争议的结论至少展示两方证据。

会变化的 API、模型目录、价格、配额或政策必须先登记到 `docs/reference/official-sources.json`。正文用 `[SOURCE:stable-id]` 绑定登记项；构建时它会显示来源状态。`verified` 只表示登记的复核者在 `checked_at` 对照一手来源核对过登记范围；若使用 LLM，必须记录 `review.method=llm`、模型身份和逐 claim 复核记录，页面会显示“LLM 已复核”。`stale`、`unknown` 和 `pending-review` 分别表示超过复核日期、网络探测失败和待语义判断。状态降级或网络可访问性不得替代语义复核。

## 贡献许可

提交贡献即表示你有权提供相应内容，并同意按仓库的双许可证规则发布：源码、配置、测试和 Notebook 代码单元采用 [MIT License](LICENSE-CODE)；正文、图表和 Notebook Markdown 单元采用 [CC BY 4.0](LICENSE-DOCS)。第三方内容必须保留来源、许可证和必要的归属信息；无法确认授权范围时不要提交。

## 提交前检查

测试不是覆盖率竞赛。修改教材 claim 或对应实现时，先按[教材测试与证据策略](docs/reference/testing.md)写清 oracle 来源、实际证明范围和不能外推的结论；新增测试必须选择证据性质，跨组件或高成本测试还要标记运行属性。

```powershell
mkdocs build --strict
python scripts/check_docs.py
python scripts/check_content_accuracy.py
python -m pytest tests/test_check_docs.py -q
python -m pytest -m "not extended and not gpu and not network"
```

`docs/`、`mkdocs.yml` 与 `overrides/` 是站点源码；`site/` 是 `mkdocs build` 的生成物，不直接编辑或提交。部署前 `python scripts/check_built_site.py` 只检查 sitemap 和静态资源结构，不锁定教材原句。
