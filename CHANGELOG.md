# Changelog

- 使用教学 Notebook、官方资料校验、安全审查与浏览器验收四条工作流做内容优化：前三本离线 Notebook 补齐读者、先修、阅读路线、完成信号、练习与证据边界；新增“追踪一个 SFT 样本”实验，从 template/shifted assistant-only labels 串到 LoRA 初始函数不变、冻结基座、adapter-only 独立重载与 held-out comparison，并以合法 next-token all-token 反例防止把当前 token 误当 target。RAG FastAPI demo 新增显式 Host allowlist、64 KiB 实际 ASGI body 上限、统一安全响应头和 dependency-based 认证，负例覆盖恶意 Host 与超限 body；生产 edge rate/body limit、TLS、真实 IAM 和未知漏洞仍不由本地测试证明。OpenAI GPT、Function Calling 与 Hosted Evals 内容按 2026-08-19 官方页面复核，明确应用执行 tool call 不替代 ACL/审批/幂等，hosted run 也不替代测量有效性和本地发布门禁。MkDocs 在桌面、390px 移动端、深浅色、抽屉、404 恢复和长表格状态完成交互与视觉检查。

- 重构 RAG 学习主线：用请求 A“为什么 ACL 必须先于排序”和请求 B“Kubernetes 灾备步骤”串联可信身份、authorization-first BM25、recorded rerank、context packing、exact-span answer、citation 与 non-empty retrieval 拒答。新增独立请求生命周期教材、先预测再运行的实验 5、可执行 walkthrough 与 RAG claim→oracle→证据边界页；重写总览、召回/重排、上下文/引用/拒答、生产化和 RAG Foundations 项目页，把固定报告与测试矩阵移出首次阅读路径。Walkthrough 测试锁定 answer coverage 1.0、no-answer coverage `2/9` 及局部证据边界，不冒充 learned reranker、LLM、语义蕴含、来源真实性、目标性能或生产安全。

- 重构推理部署学习主线：新增“一次请求如何穿过推理引擎”端到端教材，以请求 A/B 串联 admission、prefill/decode、block table、采样、SSE、四时钟和取消收口；新增 Paged KV/COW 引导实验，要求先预测 block/refcount/三份容量账本，再运行真实 CPU K/V tensor 与容量原子失败负例。推理基础、优化、vLLM 部署和 Inference Serving 项目页按“教材→引导实验→项目→证据”重新分工，精确 claim、测试与外推边界集中到独立证据页。导航、系统工程路线和 README 同步，重写页面清除既有可读性债务，同时保留所有原有实现深度与准确性门禁。

- 新增组合现有 `PagedKVAllocator` 的 PyTorch-backed paged KV tensor control：预分配 `[layers, blocks, kv_heads, block_tokens, head_dim]` arena，支持真实 K/V append/materialize、prefix fork、partial-tail COW、refcount release/recycle、MHA/GQA causal dense reference 对账与 backend 更新失败后的 poisoned fail-closed 状态。9 个测试覆盖容量原子失败、父子 tensor 隔离、shared full-tail、stale-data 清理、layout 和数值契约；CPU float64 toy 明确不冒充 CUDA PagedAttention、模型 decode、VRAM 或性能证据。

- 收敛测试调度与重复 transport 成本：1797 个 collected tests 现在都必须声明 formula/contract/security/smoke 证据性质，`extended` 仍必须给出 integration/slow 理由；将整文件继承的 105 个 smoke 收窄为 32 个真实正向入口。MCP SDK 的 8 个纯 strict-JSON loader case 回到 PR 核心层，stdio live report 改为 module-scoped 只读基准后由各 tamper case 深复制；authored MCP stdio、Streamable HTTP 与 A2A 各自合并重复 API/project-CLI 控制，只执行读者项目入口一次并保留完整 transport/protocol/scope 断言。Extended 从 27 项收敛为 17 项，未删除安全边界；本机专项由 60.07 秒降至 43.06 秒，慢例标签按实际单 case 一秒阈值校准。

- 完成 Artifact/SDK/协议测试首轮准确性审查。修正三处“`frozen=True` 但仍保留 caller-owned mutable alias”的身份漂移：evaluation run manifest 现快照 ordered case IDs，HMAC release ledger 现快照 records，model release report 现深快照嵌套 records、返回 detached `to_dict()` 并在构造时复核 projection fingerprint。测试用构造后修改原 list、清空 records 和改写导出嵌套 dict 的反例锁定身份不变。无密钥 self-fingerprint 仍只表示 canonical content identity，不认证来源；HMAC 认证、artifact rehash 与外部 trusted-head 回滚检测继续分开表述。补齐 evaluation artifact、checkpoint、release evidence、MCP memory、Responses replay、reasoning/trajectory publication 的 marker；跨独立 loader 的相同 strict-JSON payload 保留为各自边界回归，不机械删成只测一个实现。

- 完成 Agent 权限、审批、幂等、预算与副作用测试的准确性审查，并补齐 formula/contract/security/integration/smoke 分类。修正 `evaluate_agent_traces` 的 fail-open：strict JSONL loader 原本拒绝非布尔值，但直接 Python API 会把 `approved=1` 按 truthiness 当作已审批并让安全 gate 通过；现在 typed API 显式校验 enum、boolean、budget、fingerprint 与 identity 字段。审批执行身份新增独立 canonical SHA-256 fixed vector，预算拒绝与失败重放改用 handler spy 断言零额外调用；outbox 继续只声明 local at-least-once，不把模拟 provider 去重外推为远端 exactly-once。云预算测试锁定逐 attempt reserve/reconcile、未知 usage 保守入账与 transport-before-budget 拒绝，但不冒充真实 provider 定价、usage 或账单。

- 建立教材优先的测试治理：新增 formula/contract/security/smoke 证据标签及 integration/slow/extended 运行属性，PR 保留公式、数据和安全核心门禁，真实 SDK/stdio/HTTP/TCP transport cases 改由 main/weekly 专项补跑；三个 Notebook 与三平台 wheel smoke 继续进入 PR。首批审查 Attention、采样、统计、训练、数据与 RAG。新增 attention 两 key、bootstrap constant/zero-difference、policy-gradient、大 margin preference loss 和 role-tagged Jaccard 手算 oracle。修正 JAX masked loss 把全 ignored batch 静默锁成有限 0 的错误 oracle，JAX checked step 与 PyTorch MiniGPT 现在均在更新前拒绝零监督/越界 target。修正 BM25 只在逐文档评分前过滤、却用全局 IDF/平均长度泄露隐藏语料影响的问题；authorized-statistics-v2 改为先授权再计算全部统计，测试锁定隐藏文档不能改变可见 score，历史 Qwen reports 以明确 legacy identity 只读重放而不伪造新录制。新增 claim→oracle→证据边界台账；分类不声称测试能证明模型质量、生产性能、统计假设或系统绝对安全。

- 新增评测测量学专题，把 construct→operationalization→reliability/validity→estimand→power 串成一条可学习链；区分 content/construct/criterion validity、measurement/sampling/analysis unit、Cohen/Fleiss κ、MDE 与 minimum meaningful effect。新增 exact `Fraction` measurement control：两位 rater κ=1 但相对 criterion accuracy=0 的反例、multiclass confusion、paired sign-test 离散 rejection threshold/conditional power、minimum informative-pair count 和声明概率网格上的 MDE；17 个测试、术语图、学习路径、Evaluation Gate 和证据边界同步，不外推 criterion 正确性、真实标注质量、总体样本量、cluster/repeated-testing 有效性或模型改善。

- 新增 Agent 决策理论专题：把 Agent/Workflow 工程还原为 state、observation、belief、transition、utility、horizon 与 POMDP，系统讲解 Bayesian belief update、expected utility、value of sample information、planning under uncertainty、Constrained MDP、hard authorization、safety/liveness 和 guaranteed termination。新增纯 NumPy `decision_theory` reference：固定二状态 signal 得到 observation probability `0.57`、posterior 约 `[0.8947,0.1053]`，强 signal 的 EVSI/net VOI 为 `6.0/5.0`，弱 signal 为 `0/-1.0`；unconstrained utility 为 `100` 的 forbidden shortcut 仍被 hard mask 排除。有限 nondeterministic transition graph 分开验证 reachable forbidden、terminal reachable、dead end、cycle 与 all-path termination，新增 9 个测试。术语图从 213 扩展到 226 个节点，新增 13 个正文/实验绑定与六组易混淆对照，并接入 Agent 导航和 Safe Agent 项目。明确 authored probability/utility/graph 不运行 LLM/tool/provider，不证明真实校准、最优策略、安全、终止或任务成功。

- 新增检索表示学习专题：从 bi-encoder/cross-encoder、pooling 与单/多正例 InfoNCE，推进到 in-batch/hard/false negatives、DPR-style 数据流、negative-mining leakage、ColBERT MaxSim、SPLADE pooling，以及 model/exact/ANN/reranker 的误差分层。新增纯 NumPy `retriever_learning` reference，精确计算 mean-reduced loss、candidate probabilities 与 query/document analytic gradients，并用 finite difference、temperature、hard/false-negative 反事实、masked MaxSim 和 learned-sparse pooling 覆盖 12 个测试；固定 easy/hard loss 为约 `0.1269/0.6444`，漏标相关文档改用 multi-positive 后 loss 为 `0.8620→0.1688` 且梯度方向反转。术语图从 199 扩展到 213 个节点，新增 14 个正文/实验绑定与四组易混淆对照，并接入 RAG 导航和项目。明确 toy 不运行 encoder/checkpoint/ANN/GPU，不证明真实 qrels、模型质量、收敛、索引性能或生产安全。

- 修正 Tiny Transformers thread-cancellation 的 live/recorded 证据边界：live execution 现在写入当次 UTC 日期与当前 runtime identity，并由独立 observation validator 核对闭合 schema、self-fingerprint、受限行为和显式 expected runtime/date；固定离线 verifier 继续只接受 2026-08-13、Python 3.12.10 等 reviewed artifact 身份。测试明确要求依赖升级后的 live report 不得被旧 artifact verifier 自动接纳，项目说明与准确性台账同步声明两类报告不可互换；这不重录或升级既有证据。

- Citation evidence-span evaluation boundary：新增 opt-in `about-llm.citation-evidence-span-metric.v1`，对 strict JSON `claims[]` 逐项验证唯一非空 claim、supplied authorized source membership、零基/end-exclusive Python string offset、exact quote，并拒绝 duplicate key、未知字段/来源、空/重复 evidence、bool/越界 offset 与 quote mismatch。固定 1,015-byte cases / 1,138-byte answers（SHA-256 `ceb3ff9d…89e8` / `c61507ec…2661`）五例得分 `[1,0,0,0,1]`；最后一例故意让“The moon is cheese.”精确绑定 `Earth is round.` 中的 `Earth` 而仍通过，机械锁定 span identity 不等于 semantic entailment。CLI/run manifest 绑定独立 revision，新增专项/CLI 测试，并同步 Evaluation Gate、RAG 生成、评测方法、Lab、面试、简历、项目索引、知识地图与准确性台账。明确 case metadata 不自证真实 ACL/corpus provenance，fixture 不调用模型、judge、权限服务或 provider，`latency_seconds=0.0` 不构成性能证据。

- Target Qwen selected-weight packed INT4 control：复用固定 Qwen2.5-0.5B-Instruct revision 与 7-file/999,586,347-byte snapshot，加载前重哈希并在 CPU FP32 eager 下捕获真实 31-token activation；仅量化第一层 bias-free `[896,896]` `model.layers.0.self_attn.o_proj.weight` 的 802,816 个参数（全模型 0.1625%），使用 row-group 128、FP32 absmax scale、4-bit `[-7,7]`。3,211,264-byte FP32 weight 形成 401,408-byte codes + 25,088-byte scales，strict bundle 为 427,328 bytes/7.514752×；artifact `sha256:006cc9a2…0bf7` 可 strict reload 且 tamper 在 decode 前拒绝。Weight/真实 selected output/整模型末位 logits relative-L2 分别为 0.132334/0.070002/0.085138，last-logits max-abs 1.625518；单提示 argmax 恰好 17→17。Closed report `sha256:df9ee045…f5cb`，8 个测试覆盖 tiny 真执行与状态恢复、bias/module gate、recorded verifier、tamper、协同重哈希的 scope/identity/arithmetic 漂移和 CLI。同步 Qwen/推理优化教材、项目 README/页、Lab、索引、面试/简历、知识地图与准确性台账；明确它不是完整 low-bit checkpoint/runtime，不执行 fused kernel、NF4/GPTQ/AWQ、calibration、generation、GPU/CUDA/vLLM，也不证明整模型 storage/resident memory、速度、质量、有效上下文、许可、来源或生产安全。

- Safe Agent 新增真实 framework Agent-loop control：固定 `langchain==1.3.14` / `langchain-core==1.5.3` / `langgraph==1.2.10` 与 `llama-index-core==0.14.23`，分别通过 `create_agent()` 和 `FunctionAgent.run()` 执行 model→tool→model，再委托同一 canonical `AgentRuntime` 做 schema、可信资源解析、policy 与 cache。四组双框架 case 覆盖 authorized、`completed → cached` replay、cross-tenant 与 unknown-tool；后两组 scripted model 虽声称 `fixture:public`，独立 verifier 因 policy-denied 或缺 canonical receipt 拒绝完成。LangChain 以 `InjectedToolCallId` 保留 identity 并回归 postponed annotation 陷阱；当前 LlamaIndex handler 不收到 `ToolSelection.tool_id`，control 使用可信 fixture action hash，且每 case 捕获 73 次 Pydantic deprecated-field warnings。新增 4 个专项测试并同步项目页、Agent 总章、索引、Lab、知识地图、简历与准确性门禁；明确没有真实模型/provider、网络、remote effect、persistent resume、streaming/parallel/cancel、性能、质量或默认框架安全证据。

- Safe Agent 新增 LangChain/LlamaIndex proposal-only tool adapter control：真实执行 `langchain==1.3.14` / `langchain-core==1.5.3` 的 `StructuredTool` full-ToolCall path 与 `llama-index-core==0.14.23` 的 `FunctionTool` / `ToolSelection` path，共用 strict Pydantic schema，再由 canonical `AgentRuntime` 处理可信 context、resource resolver、policy、call-id cache 与 handler。固定授权、同 id 重放、跨 tenant、类型错误、unknown-tool 五组 case；其中当前 LangChain 在 framework Pydantic gate 拒绝 `key=7`，LlamaIndex direct `FunctionTool.call()` 先形成 proposal、再由 Draft 2020-12 gate 拒绝，且当前 parameters projection 省略 closed-root marker。新增 3 个专项测试，同步 Agent 总章、项目页/索引、知识地图和简历边界，并扩展内容准确性 marker；明确未执行 LangGraph/LlamaIndex Agent loop、模型/provider、网络、remote tool、异步取消或真实副作用，不能外推框架默认 ACL、幂等、生产安全、性能或跨版本兼容。

- 将 `projects/jax-minigpt/README.md` 从 43 个非空行的密集 control 汇总重构为 328 个非空行的 standalone 四层证据教程：补齐纯函数 decoder/PyTree/causal attention/masked mean、632 参数 overfit 与 JIT 同步计时、同解析权重 plain-SGD parity/RMSNorm 反例、shared-mask 三步 AdamW trajectory、`ALLMJAX1` manifest/payload/checksum、跨进程 exact resume/wrong-PRNG/wrong-cursor、故障树、Orbax/sharding/mixed-precision 扩展、验收与简历边界。README 复用站点页同一组数值/语义 markers，并新增 250 个非空行下限、run/control summary 失败测试；明确 `train_tiny.py` 不生成文本、shared mask 不证明 native RNG、file `fsync` 不证明目录 durability、CPU tiny 不外推 GPU/TPU/多设备/目标模型/性能。

- 将 `projects/rag-framework-adapters/README.md` 从 38 个非空行的高密度说明重构为 364 个非空行的 standalone canonical-first 工程教程：补齐文件/证据地图、ACL-before-ranking 公式、六个保护 metadata 键、LangChain/LlamaIndex 字段映射、LlamaIndex embed/LLM exclusion 的真实边界、安装/运行、四文档授权预测、固定 Prompt/answer fingerprints、round-trip 来源边界、16 个负例、框架公平比较、learned retrieval/LLM/async/tracing/cache 扩展、故障树、验收和简历表述。重新实跑 `langchain-core==1.5.3`、`llama-index-core==0.14.23` 双 Retriever/Prompt control 与 16 个专项测试；新增 README 300 个非空行下限、run/evidence/production/scope markers 和故意失败测试。此次只增强项目可学习性、防退化和证据解释，不新增 native embedding/index/query engine、learned reranker、LLM、网络、性能、目标向量库、GPU 或生产安全证据。

- 将 Gemini 模型页从 141 个非空行的平台/多模态概述重构为 900+ 个非空行的“闭源 L0–L5 证据→平台/API/version 身份→Interactions resource/status/steps→stateful/stateless history→typed SSE→background→`generateContent` candidates/parts→多模态反事实→tool/thought→retry/budget→评测/rollout”独立教材。于 2026-08-15 重新核对官方 Interactions overview/API reference/streaming、GenerateContent 与 text-generation：明确 GA Interactions 与 legacy-but-supported `generateContent` 的 object graph/terminal 不可混写，`output_text` 是有损 projection，stateless continuation 必须保留必要的 model-generated steps/signatures；流式路径区分 `interaction.created → step.start/delta/stop → interaction.completed → done/[DONE]` 与 `finishReason + EOF`。新增 version/safety capability drift、background/cancel reconciliation、typed part 威胁模型、模态因果反事实、三层长上下文、usage 分项、工具授权/effect、单卡本地组件、真实 smoke/rollout/简历边界；把 80/66/146 micro-USD 保持为 authored policy fixture。新增 750 个非空行下限、Interactions/generateContent/multimodal/production/scope markers 与故意失败测试；明确仓库只取得 `generateContent` text-only authored/offline L3 子集，未实现 Interactions parser，未执行 Google GenAI SDK、账号、真实网络/SSE/model、多模态/tool/thought/file/cache、usage/billing，不能外推质量、安全、性能或生产 SLO。

- 将 Claude 模型页从 137 个非空行的研究/API 概述重构为 564 个非空行的“闭源 L0–L5 证据→Messages object graph→canonical/wire 分层→content block/SSE lifecycle→tool proposal/ACL/effect→retry/outcome→逐 attempt budget→长上下文/cache→评测/rollout”独立教材。按 2026-08-12 官方 Messages reference 核对顶层 `system`、`x-api-key`、`anthropic-version`、usage/stop reason 与 `message_start → content_block_* → message_delta → message_stop`；明确 text response projection 会丢失 tool/thinking/signature/citation/media/unknown blocks，仓库 `AnthropicTextStream` 也只覆盖 text delta。把 80 uncertain + 66 settled = 146 micro-USD 固定为 authored policy fixture，不冒充 Anthropic 定价、usage 或发票；补齐工具授权、opaque reasoning 历史论文不可外推、长上下文三层上限、评测分母、发布回滚和简历边界。新增 500 个非空行下限、Messages/stream/tool/budget/scope markers 与故意失败测试；明确未执行 Anthropic SDK、真实账号/DNS/TLS/SSE/model/tool/thinking/cache/billing，MockTransport/SQLite/offline controls 不证明 Claude 质量、产品能力、安全、性能或生产 SLO。

- 将 DeepSeek 模型页从 134 个非空行的 MLA/MoE/R1 概述重构为 590 个非空行的“L0–L5 证据→固定 V3 config→MLA cache/absorption→MoE 参数/路由/EP→FP8/MTP→R1/GRPO→test-time compute/API→单卡/服务”独立教材。锁定 `deepseek-ai/DeepSeek-V3` revision `e815299…c9eb`、1,660-byte raw config、完整 raw/semantic hash 与共享 manifest/projection fingerprints；逐项审计 `auto_map`、61 layers/hidden 7168、MLA ranks/dimensions、256 routed/1 shared/8 active expert markers、FP8 E4M3 block、YaRN/163,840 与 MTP fields。明确 `7168/128=56` 不是标准 head dim，128 query/KV heads 不能绕过 MLA，verifier 必须返回 `standard_kv_applicable=false`/`estimate_refused=true`/空 estimates；`512+64` 也不发布为 cache 显存。新增 conditional expert 44,040,192 / routed layer 11,274,289,152 参数公式、remote-code 供应链、FP8/MTP 验收、GRPO/verifier/pass@k 风险、目标 runtime cache/EP 审计、单卡 fail-closed 与作品集边界，并将 authored NumPy/PyTorch/Gloo controls 保持为旁路机制证据。新增 500 个非空行下限、config/MLA/MoE/training/scope markers 与故意失败测试；明确仓库仍未下载/执行 DeepSeek weights/tokenizer/remote code、MLA/MoE/FP8/MTP/R1/API，没有参数量、有效上下文、GPU、质量、性能、许可或生产安全证据。

- 将 Qwen 模型页从 150 个非空行的家族/控制汇总重构为 578 个非空行的“L0–L5 证据→checkpoint inventory→固定 config/GQA/KV→tokenizer/template→权重执行→干预/RAG/服务/训练→单卡预算→评测/发布”独立教材。锁定 Qwen2.5-0.5B-Instruct revision `7ae5576…9a775`、659-byte config、7-file/999,586,347-byte snapshot、988,097,824-byte weight、config/checkpoint/report fingerprints，并逐项解释 14/2 heads、head dim 64、group 7、32,768-token 2-byte ideal K/V=402,653,184 bytes/384 MiB；明确 config BF16 字段不等于本仓库 CPU FP32 execution。把真实 prefill/cache/generate、activation patching、post-completion SSE、原始 RAG 0/2、guarded 1-call reject/0-call abstain、SFT final-label no-backward、LoRA 单步 loss 上升/export/reload、DPO 同 batch loss 下降/reference replay drift 分成不可互借的 L4 证据，补齐单消费级 GPU 状态账本、中文/工具/长上下文/系统评测、生产回滚、面试与简历边界。新增 500 个非空行下限、release/runtime/training/scope markers 与故意失败测试；明确仓库没有 Qwen L5 代表性质量、CUDA/QLoRA/vLLM、峰值显存/性能、32k 有效上下文、来源签名、许可适用或生产安全证据。

- 将 Llama 模型页重构为 500+ 个非空行的“发布声明→checkpoint inventory→结构/内存推导→模板→量化/微调→部署/评测→许可/供应链”独立教材；明确 L0 只是品牌标签、L1–L5 才是五级证据，系统讲解 RMSNorm、RoPE、SwiGLU、GQA、参数/KV/LoRA 公式、Base/Instruct/chat template、QLoRA、Transformers/vLLM、长上下文与单消费级 GPU 实验路线。锁定 Meta Llama 3.2 model-card commit、25,416-byte、上游/投影 SHA-256、六段 fragments 与 manifest/projection fingerprints；默认离线 verifier 保持 `upstream_verified=false`，所有 1B/3B、128k、GQA、shared embeddings、9T 与 cutoff 只标为 vendor-reported。新增 400 个非空行下限、架构/证据/边界 marker 和故意失败测试；修正 L0–L5 层级表述、RoPE cache 不属于参数账本及推理/训练降级路径混用，并明确仓库未读取或执行 Llama config/tokenizer/weights、未验证有效上下文、许可适用、GPU、质量、性能或生产安全。

- 将跨供应商 Cloud API 模型页从 58 个非空行的长段摘要重构为 417 个非空行的独立协议与生产工程教程：按 canonical business model→provider wire protocol→transport→runtime/gate 分层，逐项解释 OpenAI Chat/Responses、Anthropic Messages、Gemini Interactions/`generateContent`、typed extension、strict RequestSpec/response identity、structured output/tool proposal、opaque reasoning、retryable/replay-safe/outcome-known 三问、deadline/cancel、HTTP target、SSE framing/provider terminal、逐 attempt reserve/reconcile、cost per successful task、安全治理、adapter 目录、capability negotiation、真实接入 runbook 与故障定位。沿用 2026-08-12/14 官方来源台账和仓库 authored/MockTransport/SQLite 证据，新增 330 个非空行下限、协议/生产/边界 marker 与故意失败测试；明确 compatible 不等于语义等价，重复生成可能重复 usage/费用，CPU/offline controls 不证明真实 provider、完整 Responses、账单、质量、安全或生产 SLO。

- 将 GPT 模型页从 98 个非空行重构为 258 个非空行的“公开研究→当前产品目录→Responses object graph→typed streaming lifecycle→生产 adapter→迁移评测”独立教程；按 OpenAI 官方文档于 2026-08-14 核对 model catalog、Responses create、streaming guide 与 streaming-events reference。新增 `about_llm.openai-responses-event-replay.v1`：strict replay 3,208-byte/15-event/2-item authored SDK-shaped JSONL，重建 `天气：晴。` 与 `lookup_weather({"city":"上海"})`，对账 12+9=21 usage，锁定 input/event projection/receipt fingerprints；16 个测试覆盖错序、delta/done/terminal 漂移、refusal、incomplete/failed、opaque item、unknown field、duplicate/non-finite JSON、invalid UTF-8、截断与资源上限。同步 Cloud API 项目、模型、索引和求职表述；明确连续 sequence 是本地 evidence 规则，未执行 OpenAI SDK、HTTP/SSE/WebSocket、真实 API/model/billing，reviewed subset 不等于完整 Responses API、质量、安全或生产可靠性。

- 将 Transformers Basics 站点页从 83 行高密度 control 汇总重构为 594 行、410 个非空行的独立教程：按 byte-BPE→dense/online/cache attention→tiny Transformers→generation 三方协议→strict config/KV→immutable Llama/Qwen/DeepSeek release evidence→固定 Qwen 真实权重→activation patching→六级 MoE 递进组织内容，并加入两张证据/collective 图、公式、故障定位、面试问题和作品集验收。实跑默认 byte-BPE 得到 273 词表/17 merges/全样本 round trip，online-softmax dense 误差 `2.220446049250313e-16`，tiny GPT-2 loss `3.4888949394226074→2.1879992485046387`；离线 release verifier 和含四条双进程 Gloo controls 的 Transformers 专项为 123 passed。新增页面防退化 accuracy gate 与故意失败测试，同步项目索引和求职表述；明确 recorded target verifier 不等于本轮重跑约 1 GB 权重，CPU/JAX/Gloo/loopback 证据不得外推 GPU、NCCL、目标模型、质量或生产性能。

- 将 JAX MiniGPT 站点页从 42 行 control 汇总重构为 270 行独立教程：补齐纯函数 params/Optax/PRNG/data state 图、无 Flax decoder 参数树与 shape、causal attention、masked token mean、JIT/异步 dispatch 计时、同权重 LayerNorm/plain-SGD parity、shared-mask 三步 AdamW trajectory、`ALLMJAX1` strict checkpoint、跨进程 bit-exact resume、accelerator/sharding 扩展与求职验收。实跑 JAX/JAXlib 0.11.0 + Optax 0.2.8 CPU 的 632 参数 60-step overfit、两条 PyTorch↔JAX controls 和 13,476-byte resume artifact；新增 config/shape/context/all-ignored-loss 负例、页面防退化 gate 与故意失败测试。修正文档旧错误：`train_tiny.py` 不生成文本；`block_until_ready()` 的 CPU timing 也不外推 GPU/TPU。明确 shared mask 不证明 native RNG，bit-exact tiny resume 不证明 Orbax/TensorStore、分片拓扑、来源认证、断电 durability、目标模型收敛或性能。

- 将 RAG Framework Adapters 站点页从 30 行摘要重构为 223 行 canonical-first 集成教程：补齐 ACL-before-ranking 公式、LangChain/LlamaIndex 字段映射、LlamaIndex embed/LLM metadata exclusion、真实 Retriever/Prompt parity 报告、Prompt/answer fingerprints、公平比较协议、learned retrieval/LLM/async/tracing/生产身份扩展和求职验收清单。实跑 `langchain-core==1.5.3`、`llama-index-core==0.14.23` 的双框架 control；adapter 专项从 11 增至 16 tests，新增 rank gap、duplicate ID、NaN/±Inf/bool score 与 exclusion drift 负例；再新增页面防退化 accuracy gate 和故意失败测试。明确四文档满分不是框架质量榜，metadata exclusion 不是授权，round trip 不认证 expected 来源，CPU authored fixture 不外推 native index、LLM、向量库、GPU、线上性能或生产安全。

- 将 Single-GPU Finetuning 站点页从 133 行 control 汇总重构为 371 行独立项目教程：补齐审计身份→train-only readiness→零下载 trainer preflight→目标 template/mask/final-label→tiny PEFT 发布→固定 Qwen LoRA/DPO recorded evidence→目标 GPU SFT/QLoRA→held-out Evaluation Gate 的主线、权限边界图、交付物矩阵、故障定位与求职验收清单。实际跑通 v3 readiness 生成、2-record trainer preflight 和 7B 启发式容量估算；新增页面防退化 accuracy gate 与故意失败测试。明确 6.18 GiB 公式结果不是目标 GPU 峰值，单样本 LoRA loss 上升不能宣称改善，reference replay drift 不等于权重变化，CPU/Gloo/tiny/recorded controls 不能拼接成 CUDA/QLoRA、生产质量或性能证据。

- 将 Synthetic Data Audit 升级为 `about-llm.synthetic-data-audit.v2`：strict loader 拒绝 duplicate key/non-finite/unknown field/invalid UTF-8 与超限输入；lineage 新增 cycle member 和 internal-parent round monotonicity 诊断；artifact 绑定 1,457-byte records、341-byte mixture、required verifier/known parent/fingerprint policy、完整 audit/mixture/scope，并以 caller-supplied 输入执行 `full_local_recomputation`，拒绝 input drift、cooperative rehash 与默认覆盖。固定报告为 4 candidates / 2 eligible / 1 eligible unique，25% synthetic target / 5.0 expected repetition，fingerprint `sha256:202d8db9…bb6f4cd`；专项增至 40 tests。项目 README 重构为 307 行，站点页由 76 行扩为 386 行/279 个非空行的独立教程，并把页面 gate 提升至 220 个非空行。明确 revision overlap 不证明模型身份，unkeyed hash 不认证来源，file `fsync` 不证明 directory durability，CPU/offline evidence 不证明 teacher/student、训练 exposure、许可隐私、无泄漏、model collapse、质量或收益。

- 将文档站的 Safe Agent 项目页从 84 行协议 control 入口扩写为完整安全执行状态机：覆盖 default-deny proposal→schema→trusted resource→policy→approval→handler→effect verifier、timeout pending 与人工 reconciliation、typed loop 的预算/循环/错误 gate、approval pause/checkpoint resume、strict JSON model planner、recorded trajectory 分母、transactional outbox 的 provider-success/ack-before-crash at-least-once 重投，以及 official/authored MCP 与 A2A 的不可互借证据矩阵。新增页面防退化 accuracy gate 和故意失败测试；明确 SQLite/outbox/loopback/simulated provider 不证明签名审批、集中 IAM、远程 conformance、provider effect、exactly-once 或生产 Agent 安全。

- 将文档站的 Cloud API Contracts 项目页从 33 行概要扩写为完整云调用契约：覆盖 OpenAI-compatible/Anthropic/Gemini 字段差异、opaque reasoning context-binding matrix、trajectory publication allowlist、严格 `Retry-After`/replay/outcome policy、JSON HTTP 与三类 SSE 状态机、内存/SQLite budget、单 attempt 接线和每次 replay 独立 reservation/tombstone 的 retry orchestrator。新增页面防退化 accuracy gate 与故意失败测试；锁定 500→200 的 80 uncertain + 66 settled=146 micro-USD 账本和 hard-limit=140 的 transport-before-send 阻断，同时明确 MockTransport/AES fixture/SQLite 不证明真实 provider、usage、invoice、server cancel、key custody、跨区域一致性或 exactly-once billing。

- 将文档站的 RAG Foundations 项目页从 45 行概要扩写为完整可运行工程链：覆盖 authorization-first retrieve/rerank、exact-span answer、source-level/no-answer 指标分母、版本化 SQLite upsert/retrieve/显式 delete、online backup/strict verify/no-overwrite restore、byte 与目标 tokenizer/chat-template 两种 packing、recorded answer/trace/citation audit、persistent extractive ASGI 以及固定 Qwen 原始 0/2 failure→counterfactual policy replay→独立 guarded runtime 三层证据。新增页面防退化 accuracy gate、失败测试和求职证据要求；明确 exact span/引用语法不证明 entailment，SQLite 不外推远端向量库灾备，counterfactual replay 不冒充真实调用节省，CPU/ASGI/少量 authored Qwen case 不外推 GPU、质量、性能或生产安全。

- 将文档站的 Inference Serving 项目页从 44 行 control 摘要扩写为完整推理部署路径：分层说明固定 Qwen Transformers HTTP、authored async SSE、tiny-Transformers cooperative thread，补入 vLLM/GPU 实测 runbook、burst/constant/Poisson workload、offered/dispatch 双时钟与 coordinated-omission 边界，以及 sampling/beam/constraint/stop/speculative、continuous batching/KV preemption/allocator/prefix cache、weight/bundle/MiniGPT/KV 量化 oracle。新增故意失败命令与页面防退化测试/accuracy gate；明确 vLLM 启动命令不是已取得的 GPU 证据，CPU/loopback/录制报告不得外推目标模型、CUDA/KV 释放、容量、性能或生产 SLO。

- 将文档站的 Evaluation Gate 项目页从 30 行导航摘要扩写为独立可运行的证据链教程：覆盖 baseline/candidate score、comparison v2、`verify-comparison` artifact-only 与 `verify-evidence` full-local-recomputation 的差别、HTML 派生视图、case/cluster bootstrap、paired/cluster randomization、Holm、sequential peeking、校准，以及 HMAC chain/artifact rehash/external trusted head 三层边界；补充故意篡改测试与求职证据包。新增内容准确性防退化门禁，要求详情页保留完整 workflow、失败路径和“不证明真实模型执行/construct validity/线上因果”的证据边界；本次只增强教材入口与校验，不新增模型、provider、GPU 或生产运行证据。

- 新增 PyTorch↔JAX stochastic AdamW trajectory parity control。在已对齐的 LayerNorm/tanh-GELU/tied-embedding CPU Float32 contract 上，由 NumPy PCG64 seed `20260814` 外部物化三张 embedding inverted-dropout masks；三步真实执行 global-norm clip 0.08、AdamW beta `0.9/0.95`、epsilon `1e-8`、全参数 decay 0.03 与 `0.02→0.01→0.005` schedule。raw/clipped gradient、first/second moments、count、参数与 post-step forward 的最大差分别为 `3.129243850708008e-07/1.862645149230957e-08`、`2.561137080192566e-09/8.003553375601768e-11`、0、`2.5480985641479492e-06`、`1.564621925354004e-07`；wrong-mask 反例为 `0.06900620367377996`。两次独立进程报告同为 `sha256:68ffa809…5c609`；新增 CLI、2 个测试、accuracy gate与教材/面试/导航。明确 shared masks 不证明 native RNG/state advance，也无 norm/bias decay mask、JIT、checkpoint、CUDA/TPU/sharding、收敛或性能证据。

- 新增 JAX/Optax strict checkpoint + cross-process bit-exact resume control。`ALLMJAX1` canonical manifest + little-endian payload 保存 params、Optax count/moments、typed dropout/data PRNG、shuffle permutation/cursor 与 step，并在创建 JAX arrays 前校验 schema/order/shape/dtype/inner+outer hash/truncation；exclusive create + file `fsync` 不冒充 directory durability。两个独立 spawn workers 在第 3/6 步交接，13,476-byte artifact 为 `sha256:e9252e5dddfa4aa5…`，sample/loss/gradient/full-state 与 uninterrupted bit-exact；wrong-PRNG/wrong-cursor 负例的参数最大差为 `0.037261832505464554`/`0.03700308472616598`。新增 CLI、5 个定向测试、accuracy gate并同步教材/求职/导航；不外推 Orbax/Flax/TensorStore、distributed checkpoint、Python/NumPy/worker/accelerator RNG、CUDA/TPU/sharding、目标模型、收敛或性能。

- 新增 CPU Float32 PyTorch↔JAX MiniGPT forward/backward/SGD parity control。固定 11-vocab/2-layer/8-dim fixture 用解析 sin/cos 参数避免 framework RNG，并显式对齐 affine LayerNorm/epsilon=`1e-5`、tanh-GELU、causal mask、tied embedding、一个 ignored target 与 plain SGD。初始 logits/loss 最大差 `7.636845111846924e-08/0`，20 个 gradients 全局最大差 `2.384185791015625e-07`；一步后参数/logits/loss最大差 `7.450580596923828e-09/7.450580596923828e-08/2.384185791015625e-07`。同权重原生 RMSNorm 反事实差 `0.37747739627957344`，防止把默认模型冒充等价。新增 strict finite report、CLI、subprocess test、accuracy gate并同步教材/项目/导航；不比较 AdamW state、schedule、dropout/RNG、JIT/timing、CUDA/TPU/sharding、收敛或性能。

- 新增 two-process CPU/Gloo MoE global-capacity + all-to-all backward + SGD control。全局四个 active tokens 的 selected counts `[2,2]` 在 `capacity_factor=0.5` 下得到 per-expert capacity=1、keep mask `[F,T,T,F]` 与 kept `[1,1]`；kept-only source→owner/owner←source splits 为 `[[1,1],[0,0]]`/`[[1,0],[1,0]]`。Rank 1 是 zero-assignment source，但 empty-payload graph edge 仍触发两次 reverse collective；dropped token 0/3 的 routed output/task hidden gradient 为 0。Router SUM gradient、owner expert gradients、一步参数与 post-step forward 均和单进程 capacity oracle exact，global MSE `15.253670387373656→14.530264380025987`。每 rank authored ledger 为 payload forward/backward `4/2`、count+metadata `6`、capacity all-gather `4`、router reduce `1`；strict fingerprint `sha256:33f11f19…`。新增 CLI、subprocess test、accuracy gate并同步教材/模型/项目/Lab/面试/导航；不外推 reroute/dropless、DDP、CUDA/NCCL、目标模型、收敛、质量或生产性能。

- 新增 two-process CPU/Gloo MoE all-to-all forward/backward + SGD control。Authored autograd Function 在 forward 按 variable splits dispatch/return float payload，在 backward 交换 splits，依次把 output/gate gradients送回 owner、hidden/gate gradients送回 source；count/四列 metadata继续走不可微 collectives。每个 local squared-error sum 除 global token count=4；owner expert gradients不重复 reduce，replicated router local gradients经 SUM all-reduce 得到 `[[2.2904292655042227],[-2.290429265504225]]`。Expert weight gradients 为 `6.460938946431114/-7.209951147135929`，一步 `lr=0.01` SGD 的 gradients/参数和 post-step distributed forward都与单进程 oracle exact，global MSE `20.78017329703821→19.41091750734501`。每 rank authored call ledger 为 payload forward/backward `4/2`、count+metadata `6`、router reduce `1`；strict report fingerprint 稳定为 `sha256:f577b29d…`。新增 CLI、subprocess test、accuracy gate并同步教材/模型/项目/Lab/面试/导航；不使用 DDP、RPC distributed-autograd 或 functional wrapper，不含 capacity、optimizer state，且不外推 CUDA/NCCL、目标模型、收敛或生产性能。

- 新增 two-process CPU/Gloo MoE token-to-owner all-to-all control。Rank 0/1 各只实例化 expert 0/1，replicated top-1 router 把 rank-0 `[-1,2,-2]` 路由到 `[1,0,1]`、rank-1 `[1]` 路由到 `[0]`；source→owner/owner←source split matrices 为 `[[1,2],[1,0]]`/`[[1,1],[2,0]]`。每 rank 五次真实 `all_to_all_single` 依次交换 counts，dispatch hidden/gate 与 source metadata，return owner output/gate 与 metadata。Rank-0 return arrival global IDs `[1,0,2]` 必须 scatter 回 local order；正确输出与单进程 oracle最大差为 0，metadata-free 反事实差为 `0.8958737432590591`。逻辑 tensor payload 按 rank 为 256/160 bytes、合计 416 bytes，不冒充 wire measurement；strict report fingerprint 稳定为 `sha256:51c77e24…`。新增 CLI、专项测试与独立 accuracy gate，同步核心 MoE/分布式教材、项目、Lab、面试、DeepSeek/Qwen、证据台账和导航；不外推到 CUDA/NCCL、目标模型或生产性能，也不含 capacity/drop、backward/optimizer、收敛或质量证据。

- 新增 two-process CPU/Gloo distributed MoE capacity-group control。两个 spawn workers 经 temporary FileStore 建组；rank 0/1 分别持有 `[[2],[1]]` 与 `[[3],[0.5]]`。真实 tensor `all_gather` 形成 replicated 4-token global routing batch，两个 `all_reduce(SUM)` 让两 rank 都观察 active count=4、pre-capacity selected counts `[4,0]`。在 2 experts/top-1/`capacity_factor=0.5` 下，rank-local 独立 masks 都为 `[T,F]`、合计 kept=2；global score-priority capacity=1 只保留第三个 token，mask `[F,F,T,F]`、counts `[4,0]→[1,0]`、pre/post excess `[[3,0]]→[[0,0]]`、drop=3。Rank 0 collective/local output 差为 `0.9640275800758169`，rank 1 为 0。父进程验证 worker PID 不同但不发布 raw PID；global-route fingerprint 固定为 `sha256:71a66eeb…`，strict report fingerprint 连续运行稳定为 `sha256:9e342b0b…`。新增 CLI、Gloo 测试、accuracy gate，并同步 MoE/分布式教材、项目、Lab、面试、DeepSeek/Qwen 与导航。Router/experts 仍被复制；无 expert ownership、token `all_to_all`/`reduce_scatter`、distributed backward、CUDA/NCCL、多节点、目标 checkpoint、性能、收敛或质量证据。

- 将 trainable MoE control 升级为 v3 explicit overflow-policy contract。固定 4-token/top-1/capacity=2 fixture 的完整稳定 ranking 均为 `[0,2,1]`，初始 counts 为 `[4,0,0]`：drop 变为 `[2,0,0]`、dropped=2；deterministic full-ranking reroute dispatched `[[0],[0],[2],[2]]`、counts `[2,0,2]`、rerouted=2、dropped=0、post-policy excess=0；dropless 保持 `[4,0,0]`、dropped=0 并报告 nominal-capacity excess `[[2,0,0]]`。Reroute 按原 selected score/token/rank 处理 dropped slots、扫描完整 ranking、禁止 token 内重复，并按 CPU-local routing group 独立计容。重归一化的 weight sums 为 `[1,1,1,1]`；以原 selected top-k sum 为分母保留 mass 时为 `[1,1,0.44932896411722156,0.44932896411722156]`，输出最大差 `0.06399997177521191`。Reroute/dropless sparse—dense output 与 materialized-zero 全参数 gradient 差均为 0；drop—reroute、reroute—dropless 输出差分别为 `0.11622178688336826`、`0.10698215447093767`。报告 schema 升为 strict finite JSON v3，MoE 专项增至 68 tests，accuracy gate、教材、项目、Lab、面试、DeepSeek/Qwen 边界和导航同步。三策略均为 authored teaching contracts，不是框架默认；仍无 distributed capacity collective、shared/fine-grained experts、all-to-all/grouped-GEMM/GPU、目标 checkpoint、收敛、质量或性能证据。

- 将 trainable MoE control 升级为 v2 padding/routing-group contract。新增 boolean token mask 与 int64 CPU-local group IDs；padding 不参与 capacity、drop 分母、sparse dispatch、per-group balance/z-loss、routed output 或 hidden gradient。固定 `[T,T,T,T,F]`、groups `[10,10,20,20,999]` 中两个 active groups 各含 2 tokens、capacity 各为 1；逐组 counts `[2,1,1]→[1,1,1]` 与 `[1,1,2]→[1,1,1]`，全局 `[3,2,3]→[2,2,2]`。Grouped sparse/dense 输出和全参数 gradient 最大差均为 0；相对 single-group capacity=2 的输出最大差 `0.3293871976258794`。修改 padding value/group id 后 active output、balance、z 差均为 0，padding output/hidden gradient 也为 0。逐组 balance/z 按 active-token 数加权；报告不把 group label 冒充 distributed collective。专项增至 58 tests，并同步项目、教材、Lab、面试、模型边界与 accuracy gate；v2 当时仍无 reroute/dropless、shared/fine-grained experts、all-to-all/grouped-GEMM/GPU、目标 checkpoint、收敛、质量或性能证据。

- 新增并扩展 trainable top-k MoE router/MLP gradient control。PyTorch 2.13.0+cpu/Float64 的 5-token、3-expert、stable top-2 fixture 真实执行三组 bias-free `Linear(3,4)→Tanh→Linear(4,2)` experts；selected-only sparse dispatch 与全 token×expert dense masked oracle 对 `MSE + 0.05×balance + 0.001×z` 的输出最大差 0、所有参数 backward 最大差 `6.938893903907228e-18`。同一训练图加入 score-priority capacity/drop：`factor=0.5` 得到 capacity=2、counts `[4,3,3]→[2,2,2]`、4/10 assignment drop，capacity-enabled sparse/dense 输出与全参数梯度最大差均为 0；两种 post-drop policy 的输出最大差为 `0.1255417263895207`，capacity=1 同分 fixture 又验证后两个全丢 token 的 routed output 为零。一次 SGD step 同时改变 router/三个 experts，authored MSE `0.08864729306070791→0.08755795603512319`，但明确不外推收敛。Detach selected gate 的负例保持 hard routes/expert gradients，router task gradient 却缺失；collapsed top-1 balance control 在 assignments 仍全选 expert 0 时把 `E×Σ stop_gradient(f_e)×mean(p_e)` 从 `2.5677239505126708` 降至 `2.552750704884368`。新增 strict finite JSON/fingerprint、稳定 tie-break、capacity、两种 post-drop policy、全丢 token、sparse/dense forward/backward、失败输入与 scope 测试，并同步 MoE 教材、项目、Lab、面试、DeepSeek/Qwen 边界、知识地图和 accuracy gate。该初版 control 当时仍不含 reroute/dropless、shared/fine-grained expert、all-to-all/grouped-GEMM/GPU、目标 checkpoint、收敛、专门化、质量或性能证据。

- 扩展真实跨 PID consumed—optimizer-committed control 为六进程 stochastic RNG/StepLR 对照。两个 spawn DataLoader workers 配合 CPU Float64 `Linear(2,1)`、seed `20260815` 的 main-process inverted-Bernoulli mask、MSE、`SGD(momentum=0.9)`、`StepLR(step_size=2,gamma=0.5)` 与 accumulation steps=2；固定 permutation 在第三个 microbatch stochastic backward 后观察 sampler emitted/main-loop consumed/optimizer committed=`7/3/2` 与两个 in-flight gradient tensors。8,985-byte base checkpoint 不含 `.grad`，但保存 commit-boundary model/optimizer/scheduler/Torch RNG：从 committed=2 恢复 RNG并 replay sample `1` 后，ledger 和终态四类组件与 uninterrupted bit-exact、参数差 0。第一个隔离负例从 consumed=3 使用正确 crash RNG却漏 gradients/sample `1`；未来 mask 与终态 RNG 相同，即使两边 optimizer/StepLR step 都为 5、LR 同为 `0.0125`，参数最大差仍为 `0.005767858566116724`。7,905-byte sidecar 绑定 base SHA-256、数据/permutation、三种 cursor、pending `[1]`、position=1、steps/loss divisor=2、两个 finite gradients与 crash-observed Torch RNG；第五个 PID 从 consumed=3 恢复，首个窗口 `[1,7]`，终态同样 bit-exact、参数差 0。第六个 PID 恢复完整 gradients/ledger，却错误使用 commit-boundary RNG；step/LR 仍相同，参数最大差为 `0.017878893573032573` 且终态 RNG 不同。sidecar 协议最后发布 827-byte strict canonical manifest，closed schema 绑定 dataset 与两个 artifacts；loader 在任何 `torch.load` 前验证 complete/identity，并对实际反序列化 bytes 重查 hash。父进程 fault matrix 拒绝 base-only、两 payload 无 manifest、manifest 缺 sidecar 与 post-manifest tamper；base-only 仍可供 commit replay。8 个测试另覆盖 duplicate/noncanonical/unknown manifest、manifest no-overwrite、sidecar/base digest/RNG、scheduler/RNG schema、cursor/ledger/loader-contract、non-finite model、missing momentum、size cap 与 scope；教材、项目、Lab 4K、面试、生产清单和 accuracy gate 同步。两种正确协议和两个隔离负例只证明当前 stochastic CPU fixture 与 process-crash completeness gate；base+sidecar+manifest/sample/optimizer 并不原子，且无 directory `fsync`/断电/filesystem fault、来源认证/不可变快照、queue/worker/Python/NumPy/CUDA RNG、原生任意随机层、GradScaler、distributed/CUDA、目标 Trainer、性能、收敛或质量证据。

- 新增真实跨 PID DataLoader worker/prefetch resume control。四个不同顶层进程分别执行 uninterrupted、phase-1、从 application-consumed cursor 恢复与从 sampler-emitted cursor 错误恢复；每段以 `num_workers=2,prefetch_factor=2,batch_size=1,multiprocessing_context="spawn",in_order=True` 启动真实 CPU workers。固定 permutation `[8,3,1,7,0,9,4,2,6,5]` 中，main loop 只收到前三条时 sampler 已 emitted 到 7，queue 中未交付 IDs 为 `[7,0,9,4]`；从 consumed=3 恢复完整顺序，从 emitted=7 恢复的组合仅 `[8,3,1,2,6,5]`。同 seed phase prefix exact，但 fresh worker-local Torch RNG tail 最大差约 0.654431；按 `(namespace,sample_id)` 派生的 stateless RNG tail 差为 0。canonical strict-JSON checkpoint 同时保存两种 cursor、数据/permutation/loader contract，5 个测试覆盖真实 subprocess、duplicate/non-finite/noncanonical/cursor drift 与拒绝覆盖。教材、项目、Lab 4J、面试、知识地图、生产清单和 accuracy gate 同步说明：consumed 不是 optimizer committed；sample-only key 未覆盖 epoch/visit；ahead=4 不是跨版本公开 API。无 queue/worker-state serialization、persistent/IterableDataset、collator/model/optimizer、DistributedSampler、CUDA、吞吐或质量证据，也不能与 model checkpoint 拼成完整 exact resume。

- 新增真实双进程 CPU/Gloo DDP + AMP overflow 共识 control。三条路径共享 finite AdamW/StepLR warm-up：rank 0 在首个 `no_sync` micro-batch 产生 Inf 时，末批 built-in DDP reduction 让两 rank 都 non-finite，共同 skip optimizer/scheduler 并把 scale/tracker 从 `8/1` 变为 `4/0`；若先完成两边 scaled grad=8 的 finite reduction，再于 rank 0 的 `unscale_` 前 authored 注入 Inf，rank-local GradScaler 会令 step `[1,2]`、parameter约 `[0.99,0.985]`、LR `[0.005,0.0025]`、scale `[4,8]`，moments/scheduler/tracker 也分叉；optimizer 前对 unscaled local flag 做 `all_reduce(MAX)` 后，两边共同 skip 且显式统一 scale=4，model/optimizer/scheduler/scaler state 保持一致。报告保存完整 scaler state 并以 `null` 表示 non-finite，1 个 subprocess test 与内容门禁锁定三路径和 strict JSON。教材、项目、Lab 4I、面试、知识地图、生产清单和准确性台账同步说明：post-reduction 损坏是 authored fault，不是 DDP 常态；`update(new_scale=...)` 保留 tracker=1，只是共同 scale policy，不等于 native found-inf transition。证据仅限 PyTorch 2.13.0+cpu、单参数/单 bucket、同机和人为 Inf；无 clipping、自然 overflow、custom hook、checkpoint/elastic、CUDA/NCCL、多节点、FSDP/ZeRO、目标 Trainer、性能或质量结论。

- 新增真实 CPU FP16 autocast/`torch.amp.GradScaler("cpu")` 顺序与恢复状态 control。两个 micro-batch 的 scaled gradient 为 24；正确 `unscale_→global-norm clip` 得到 `3→约 0.5` 并与 full batch/SGD 参数一致，故意 `clip→unscale_` 则把 optimizer-visible gradient 错缩到约 `0.0625`。真实 AdamW 先建立 step=1 与一/二阶矩，再由三个含有限+`inf` micro-batch 的窗口观察 scale `8→4→2→1`，每个 overflow 都跳过整个 update 且参数/moments/step 不变。in-memory split point 同时复制 model/optimizer/scaler：恢复 scale=1 后 10000 的边界梯度执行 AdamW step=2 并与不中断路径 exact；漏恢复 scaler、回到 scale=8 时 FP16 scaled gradient 溢出、scale `8→4` 且 step 仍为 1。6 个测试覆盖正确/错误 clip 顺序、overflow、非空 AdamW state、scaler omission 与 strict finite JSON；教材、项目、Lab、面试、知识地图、生产清单和准确性门禁同步更新。证据仅限 PyTorch 2.13.0+cpu 单参数 fixture 与进程内 state replay：没有文件 checkpoint/真实进程重启、scheduler/RNG/DataLoader/DDP/FSDP、CUDA/GPU、目标模型/Trainer、性能、收敛或质量结论。

- 新增 DDP accumulation/`no_sync`/clip/optimizer-update 精确 oracle 与真实双进程 CPU/Gloo control：固定 `D=2`、每 rank 两个 micro-batch、valid counts `[[1,2],[3,1]]`、`N=7`，推得正确 local sum scale `D/N=2/7`、full/one-final-sync/sync-every-batch gradient `(+19/35,-19/35)` 与未裁剪 plain-SGD `lr=7/20` delta `(-19/100,+19/100)`。真实 built-in DDP 将首批 forward+backward 一起置于 `no_sync`，末批同步后执行 `max_grad_norm=0.5` 和 plain SGD；pre/post-clip gradient、参数更新与单进程 full batch 完全一致。另以 PyTorch 官方 reference all-reduce hook 做计数正/负对照：正确 scope 1 次 hook，只包 backward 2 次；后者在当前线性 fixture 上数值仍正确，只证明没有省通信。12 个测试覆盖精确嵌套值、输入/resource gate、真实 subprocess、clip/update 与 scope；教材、项目、Lab、面试、知识地图、生产清单、wheel smoke 和准确性门禁同步更新。built-in collective count 未直接插桩，且无多参数/bucket、随机层、AMP、AdamW、FSDP/ZeRO、GPU、多节点、目标模型、性能或质量证据。

- 新增 default-DDP global token-mean 精确 oracle 与真实双进程 CPU/Gloo control：固定 `D=2,N=4`、rank counts `[1,3]`，证明默认 DDP gradient mean 下 local loss sum 应乘 `D/N=1/2`，同步 shared-bias gradient 为 `(+23/40,-23/40)`；漏 world size 的 `1/N=1/4` 得到 `(+23/80,-23/80)`，rank-local mean 得到 `(+7/20,-7/20)`。两个 `spawn` OS processes 经 temporary FileStore/Gloo 真实 `all_reduce` count=4 并执行 `DistributedDataParallel.backward()`，两个 rank 观察相同梯度；正确路径相对 full-batch 最大误差约 `1.11e-16`。7 个定向测试覆盖精确三路径、输入 gate、JSON 与 subprocess control；基础、预训练、SFT、分布式、项目、Lab、面试、知识地图、生产清单、wheel smoke 与准确性门禁同步区分单进程 toy 和 reducer 集成证据。没有 optimizer/update/clipping、accumulation + `no_sync`、AMP、FSDP/ZeRO、GPU、多节点、目标模型、性能或质量证据。

- 新增 masked-token gradient accumulation 精确控制：整数 probability weights + `Fraction` oracle 证明 token mean 必须按有效 token count 加权 micro-batch mean；固定 `[1,3]` token 反例中 full/count-scaled class-aggregate gradient 为 `(+23/40,-23/40)`，等权 micro-batch mean 为 `(+7/20,-7/20)`，差为 `(-9/40,+9/40)`。项目 toy 真实执行 PyTorch CPU Float64 `cross_entropy.backward()`，full 与逐批 sum/全局 count 梯度 `torch.equal`，三个 ignored rows 为零；18 个测试覆盖精确数学、loss、输入 gate、资源上限、JSON 与 CLI。基础、预训练、SFT、分布式、项目、Lab、面试、知识地图和准确性台账同步区分 token/sequence/micro-batch estimand，并说明 DDP reducer/world-size、RNG、BatchNorm、AMP、clip、scheduler 与 `no_sync` 条件。没有 optimizer、目标 LLM、distributed runtime、CUDA、性能或质量证据。

- 新增 binary self-consistency correlation 精确控制：每题抽一次 finite latent regime、候选在 regime 内 conditional i.i.d.，以 `Fraction` 闭式计算 odd-N binomial majority、marginal success 与 pairwise covariance/correlation。Independent 与 easy/hard-correlated 场景的边缘单样本成功率都为 0.6，N=11 多数票却分别为 0.75349813248 与 0.53896454244，后者 correlation=`3/8`；小 N 显式枚举与闭式对照。教材、推理项目、Lab、面试、知识地图、术语表和准确性台账同步强调该 counterexample 只覆盖二元 canonical labels，不处理开放文本 plurality/canonicalization，也没有 model/tokenizer/dataset/judge/GPU/provider、质量、latency 或 cost 证据。

- 新增 verifier-guided best-of-N 精确控制：有限 authored candidate distribution、i.i.d. 抽样、deterministic score 和显式 `(verifier_score, candidate_id)` tie-break 下，用 `Fraction` 闭式计算 `P(select i)=F_i^N-F_{i-1}^N`、oracle@N、selected@N、两者 gap 与 expected verifier score。固定 N=1/4/16 反例中 oracle 从 0.4 升至 0.9997178890、proxy 从 51.9 升至 95.4783461，selected success 却先升至 0.5936 再降至 0.1852867601；小规模枚举测试对照闭式公式。教材、推理项目、Lab、面试、知识地图与准确性台账同步区分 logical samples/scores 和 wall-clock/cost/parallelism，并明确没有执行 model/tokenizer/PRM/GPU/provider，不证明 calibration、语义正确或目标模型质量。

- 补强微调端到端决策与证据边界：按知识、行为、协议、系统和治理失败选择 Prompt/RAG/SFT/DAPT/偏好训练或 runtime 控制；新增数据接口→机制执行→训练目标→held-out 行为→发布运行五层证据阶梯、可证伪实验合同与训练 checkpoint/服务 artifact 分账。实验 4 与训练准确性门禁同步要求逐层 artifact、阈值和不可外推项，明确单步 backward、同 batch loss、adapter 重载、merge 或量化任一项都不能单独证明微调质量与生产就绪。

- 扩写服务与可观测性为生产控制面章节：区分 data/control/evidence plane，新增 eligible offered 分母、error budget、版本化 workload contract、typed terminal、logical call/attempt/idempotency identity、token/KV/queue admission、capacity release 证据、FCFS/长度 lane/priority/tenant fairness、多 worker/replica 隔离、autoscaling/cold-start/drain、跨 clock-domain tracing、shadow/canary/rollback、runbook 与容量实验协议。证据矩阵明确 attempt 统计、continuous batching、KV preemption、固定 Qwen service、authored incremental SSE 和 tiny Transformers thread controls 是不同局部命题，不能拼接成 vLLM/CUDA/生产容量实证；实验 7、系统面试题、知识地图和系统准确性门禁同步更新。

- 重构岗位路线与求职地图：按主要交付物和失败责任区分 LLM 应用、RAG/搜索、Agent 平台、模型训练、推理系统、评测数据与安全治理七条路线；用 K/I/V/O（知道、实现、验证、负责）替代按年限自评，新增背景迁移、JD 动词/对象/规模/生命周期/评价拆解、主项目+相邻项目+底层实现组合、证据等级与简历反向审计、面试环节、故障回答、职级 ownership、团队反向评估和 decision record。页面明确不是周计划，也不把 CPU fixture、loopback、目标 checkpoint 单点 control 或 L4 生产设计升级成 GPU/线上/组织级实证；career 准确性门禁新增十一项稳定边界。

- 重构六大模型家族总览：不再停留在品牌简介，而是用研究主张、模型家族、发布 artifact、产品 API、执行配置和应用系统六层对象定义可执行候选；新增开放权重/开源分账、hard gate 与 Pareto 决策、RAG/Agent/SFT/推理/多模态/代码固定协议、单 GPU 容量与云费用账本、provider adapter 差异、可复现 manifest、迁移回归、面试题和实践任务。准确性门禁新增十项稳定边界，避免回退为无日期的榜单或品牌级架构外推。

- 修复 tool-aware SFT v2 升级后的两处契约漂移：CLI 测试不再期待 readiness v2，隔离 wheel smoke 不再期待 target SFT control/report v1；新增 source export→smoke expectation 回归测试和内容门禁。知识地图同步改为三条固定 SFT 记录（多轮 assistant、并行 tool calls、tool preamble），不再沿用旧的“两条单 assistant fixture”边界。

- Tool-aware SFT v2 与 Target Qwen final-label control：严格 SFT contract 现支持 function tool definitions、空正文 assistant tool calls、并行 call ID、tool response 绑定和完整 lifecycle 校验；near-duplicate/governance/readiness v3 将 tool calls/schema 纳入身份与候选扫描。正式入口在 Hugging Face Arrow 接触异构 nested arguments 前完成 template/tokenizer/mask，避免 Arrow struct 注入 `null` 改写 prompt；由于 TRL 0.29.1 会拒绝“已预分词 Dataset + `assistant_only_loss=True`”，入口显式使用 `assistant_only_loss=False` 并让同一真实 collator 消费预计算 `assistant_masks`，训练前另写 final-label audit。固定 Qwen control 复用 7-file/999,586,347-byte snapshot、2,337-byte tool-aware train、2,570-byte held-out-free readiness 与 2,793-byte 审核模板；原生模板在多轮、并行 tool calls、tool preamble 三条记录上 mask 全零，审核模板保持 47 / 301 / 200 个 input IDs 完全相同并标出 8 / 51 / 31 个 assistant tokens。TRL batch 为 `[3, 301]`、548 attention tokens、355 padding tokens、90 个监督 labels、813 个 `-100`，CPU FP32 no-grad loss `1.251716136932373`。manifest `sha256:b1c1a6b3…936e6`、report `sha256:8b61fa58…10421a`。没有执行 backward、optimizer、LoRA、QLoRA、CUDA 或 vLLM；固定 schema/fixture 不证明任意 provider tool 格式、tool 执行/结果真实性、数据合法性、质量、收敛或安全，verify→loader reopen TOCTOU 仍存在。

- Target Qwen DPO control：复用固定 Qwen2.5-0.5B-Instruct 7-file/999,586,347-byte snapshot，并绑定 1,325-byte binary train 与 2,895-byte held-out-free readiness；目标 tokenizer/TRL collator 固定 `[4,28]`、112 attention tokens 与四个 5-token completion masks。CPU FP32、TRL 0.29.1/PEFT 0.20.0 下真实执行 270,336-parameter `q_proj/v_proj` LoRA DPO backward 与一次 AdamW step，96/96 gradient tensors finite，48 个 B tensors 共 98,304 elements 全非零；loss `0.693147→0.333352`，两条 relative margin `8.566292/10.016453`。冻结 494,032,768 基座参数、non-adapter state、规范化 model/generation config 指纹前后 exact；reference forward 内 adapter 状态均为 disabled，但 replay max-abs drift `0.547077`，报告明确不把它等同于权重变化或 bitwise determinism。manifest `sha256:ebbf3655…d818b3`、report `sha256:3cafbade…b549b7bc`；14 个定向测试覆盖 tiny 真实 Trainer、strict manifest、recorded verifier、输入 tamper、协同 rehash 的 runtime/scope/result/special-token 漂移与 CLI。authored `good/bad` pair 不证明人类偏好、held-out 质量、收敛、安全、CUDA/QLoRA/vLLM 或生产就绪。

- Target Qwen activation-patching control：复用固定 Qwen2.5-0.5B-Instruct revision 与 7-file/999,586,347-byte selected snapshot，在加载前重哈希，并以 CPU FP32 eager/`trust_remote_code=False` 对 26-token France/Germany chat pair 真实执行 10 次 forward/hook。协议固定 position 19 source、position 25 的 `Paris−Berlin` metric 和 first/lower-middle/final layer 0/11/23；clean/corrupt metric 为 9.210311/-7.700302，source recovery 为 1.000024/0.992244/0。完整 layer-0 prefix 与 final-layer readout 正对照精确为 1/1，future-position 与 final-layer-source 因果结构对照为 0/0，hooks 全部移除；report 为 `sha256:3f8410f5…ebb18c`。新增 optional-dependency-safe 模块、8 个定向测试、wheel import smoke、机器报告与教材/Lab/准确性门禁；v1 verifier 在攻击者协同重算无密钥 self-hash 后仍拒绝 result/scope 漂移。明确 tokenizer metric 在 templated baseline 后校准，属于 authored fixed protocol 而非外部可信时间戳 preregistration；单事实、batch 1、整条 896-d residual 干预不证明事实存储层、唯一自然 circuit、总体事实性、SAE、CUDA/vLLM、性能或生产安全。

- Tiny Transformers thread-cancellation control：真实 Uvicorn subprocess/IPv4 loopback HTTP 中构造随机 1,272 参数 GPT-2，thread 内执行一次 CPU forward 与 `GenerationMixin.generate()`；authored streamer 在首 token 7 后暂停，client close→backend `CancelledError`→`threading.Event`→authored `StoppingCriteria`，最终 continuation 仍为 `[7]` 且 thread exit/join。Uvicorn 0.52.1 重录后的 closed-schema report `sha256:eadcab54…f62bc7` 与 10 个测试覆盖真实断连、离线 verify、协同 rehash、strict JSON/UTF-8/size 和 CLI。明确 streamer pause/cooperative hook 是控制机制，不证明未修改/已阻塞调用、目标 checkpoint/logits、vLLM/CUDA、KV/CPU/GPU release、provider billing、性能或质量。

- Incremental SSE/disconnect cancellation control：新增 streaming-only OpenAI-style reference path、authored cooperative async pseudo-token backend 与真实 Uvicorn subprocess/IPv4 loopback HTTP。完整 case 证明 content 在 backend 完成前到达并核对 finish/usage/`[DONE]`；取消 case 在首 delta 后读取 active/incomplete audit、显式关闭 client response，再验证 ASGI task/backend iterator 均观察 `CancelledError`、后续 token 未产生。Uvicorn 0.52.1 重录后的 closed-schema report `sha256:25846822…2b5d00` 与 13 个测试覆盖 protocol drift、协同 rehash、duplicate/nonfinite/invalid UTF-8 和 CLI；明确不执行 tokenizer/model、Transformers blocking thread、vLLM/CUDA、KV/GPU release、TLS/IAM、远程/多 worker、provider billing、性能或质量。

- Target Qwen HTTP reference service control：新增 closed-schema、strict UTF-8 finite JSON 的 `/healthz`、`/v1/models` 与 `/v1/chat/completions`，以及 Bearer、单 worker admission、non-stream/SSE/usage/finish 和内容无关 audit。Uvicorn 0.52.1 重录后的 service manifest `sha256:cfb9b540…8fb4ea` 绑定既有 Qwen2.5-0.5B-Instruct 7-file/999,586,347-byte snapshot、31-token prompt、continuation `[17,151645]` 与 reviewed runtime；真实子进程/IPv4 loopback HTTP 各执行一次 non-stream/SSE，后端实际 `generate()` 2 次，recorded report 为 `sha256:63e566ca…617ddb`。10 个测试覆盖 reference API、fake-backend real-TCP driver、strict manifest、offline verifier、协同 rehash 篡改与 CLI；真实 control 另重哈希并加载目标权重。明确它是 CPU FP32 eager reference，不是 vLLM/CUDA/性能/完整 OpenAI compatibility；SSE 在 generation 完成后才发送，不证明 incremental decode/cancel，且无 TLS/IAM/远程/多 worker/质量/许可/来源认证/生产安全或 TOCTOU 消除证据。

- Target Qwen LoRA control：复用固定 `Qwen/Qwen2.5-0.5B-Instruct` revision 与 999,586,347-byte selected snapshot，在每次加载前重哈希，CPU FP32/eager 下真实执行 41-token prompt/3-token assistant-only supervision、24 层 `q_proj/v_proj`、`r=4/alpha=8` 的 backward 与单次 AdamW step。270,336 个 adapter parameters 的 96 个 trainable tensors 都有 finite gradient，494,032,768 frozen base 的前后 fingerprint 同为 `sha256:716454a9…e7092`；48 个 B tensors 全非零。标准 PEFT safetensors 为 1,093,728 bytes，strict artifact manifest `sha256:ffab4958…c96c46` 绑定 file/config/layer/A-B/shape/dtype/value scope；新加载的同 revision 基座重载 adapter 后 logits max error=0，report `sha256:8a3897b1…026230`。12 个测试覆盖 tiny Qwen 真 backward、完整 runner 初载/发布/重载、自验、recorded evidence、tamper、协同 rehash、duplicate/nonfinite/UTF-8 与 CLI。保留 initial/post loss 约 `0.003864→0.584557` 的上升，明确 plumbing success 不等于收敛/质量；无 optimizer/RNG resume、merge、QLoRA/CUDA/AMP/vLLM、显存/性能、来源认证、许可或生产证据。

本项目参考 Keep a Changelog 风格记录重要变化。教材是一次性成体系建设，日志用于追踪勘误、实现进度和兼容性，不承诺实时追逐模型榜单。

## [Unreleased]

### Added

- Strict structured-output evaluation metrics：新增 `about-llm.json-schema-metric.v2` 与 `about-llm.json-value-exact.v1`。Schema v2 将输出切到 strict JSON，拒绝 duplicate object key、`NaN/Infinity`，只允许 local `$ref/$dynamicRef` 并拒绝 `$id`/external resolution；invalid schema 作为 case 配置错误。Value v1 对 strict parsed JSON 做 canonical equality，忽略 object key order/whitespace，保留 array order、scalar type 和 integer/float parser distinction。新增 1,474-byte cases + 432-byte answers 五例 fixture：object-order 行为 `0/0/1/1/1`、wrong value schema/value=`1/0`、duplicate/nonfinite schema=0、reversed array F1/schema/value=`1/1/0`；CLI/run manifest 绑定独立 metric revisions，单测与内容门禁复算完整矩阵。明确 `format` 仍是 annotation，不 coercion/default；value equality 不等于业务语义/授权，`latency_seconds=0.0` 是非性能占位，fixture 不执行模型/provider 或代表性质量评测。
- Target Qwen behavior-evaluation control：复用固定 `Qwen/Qwen2.5-0.5B-Instruct` revision、checkpoint manifest 与 7-file/999,586,347-byte snapshot，加载前逐文件重哈希；CPU FP32/eager、batch 1、greedy、`max_new_tokens=12` 下真实执行 7 次 `GenerationMixin.generate()`，保存 raw output、prompt/continuation token identity、EOS/cap 和 deterministic slices。Suite/report 分别为 `sha256:27ada9b1…6201`/`sha256:dd30a278…5c43`；中文算术/北京/Paris/空证据拒答通过，英文算术输出 `112`，大小写复制输出 `llm-2026`，JSON 输出 `{"answer": 42}`，literal exact/normalized exact/token F1 为 `4/7`、`5/7`、`6/7`。新增 literal exact 是因为真实输出暴露 `casefold()` 会掩盖大小写错误；JSON case 又显示 token F1 会忽略结构标点/空白。通用 scorer 同步新增 opt-in `--metric literal_exact_match` 与独立 revision，兼容默认仍为 normalized exact + token F1；target control 复用同一实现，避免 scorer 分叉。10 个相关测试覆盖真实 tiny generate、closed suite/report、reviewed fingerprint、tamper/协同 rehash、无权重 CLI verify、literal/normalized 反例与 CLI manifest revision；教材、Lab、求职、知识地图、准确性台账和内容门禁同步接入。明确 authored suite 未外部预注册、未独立抽样/留出、非代表性、无统计功效；无 judge、人评、系统比较、release gate、latency/性能、GPU/vLLM、总体质量或生产证据。
- Cross-process CPU AMP training-resume control：新增真实 `torch.save` checkpoint 与 8 个独立 worker 进程，把 6 参数 authored model、AdamW、StepLR、CPU GradScaler、Torch/Python RNG、stateful shuffle generator/permutation/cursor/epoch、progress 和 dataset hash 放在同一 split-run。phase-1 在 1 次有限 update、3 次 overflow（scale `8→4→2→1`）后写文件并退出，不同 PID 用 `weights_only=True` 重开；resume tail 与最终七类 component state 都和 uninterrupted worker bit-exact。五条 counterfactual 分别验证 overflow 上错误推进 scheduler、漏 scheduler/scaler/RNG/data state 会造成不同且可定位的漂移。边界明确为 PyTorch 2.13.0+cpu、custom stream、zero-grad boundary 与 pickle-backed authored local artifact；不冒充 DataLoader worker、accumulation/distributed/CUDA/目标 Trainer、crash atomicity、来源认证、质量或性能证据。
- Blockwise online-softmax correctness oracle：新增 float64 running-max/normalizer/value recurrence，不物化完整 score/probability，支持广播 leading dimensions、causal prefill、带 past keys 的 decode 与任意 boolean mask；全遮挡、非法 shape/dtype/mask/block size 和非有限输入 fail closed。固定 5×7、block-size 3 demo 的 3 个 blocks 把最大 logical score tile/full score 分账为 15/35 elements，和 dense reference 的最大绝对误差为 `2.220446049250313e-16`；29 个定向测试覆盖多 block size、稀疏 mask、大 logits、dtype、项目 CLI 与证据边界。教材、Lab、面试、知识地图、wheel smoke 和内容门禁同步接入；明确 logical tile 不是进程/显存峰值或 HBM 测量，NumPy CPU oracle 不证明 FlashAttention/CUDA/vLLM backend、性能或逐 bit 等价。
- MCP 2025-11-25 official-SDK memory control：固定 `mcp==1.29.0`，以 AnyIO in-memory streams 真实执行官方 `ClientSession`、low-level `Server`、generated types、初始化/发现与 JSON Schema validation；成功返回结构化结果，schema-invalid 在应用 handler 前拒绝，而 unknown tool 进入应用 handler 后由 allowlist fail closed。6 个测试覆盖闭合报告、协同 rehash 语义漂移、项目入口和 raw error 不公开；教材、实验、项目、面试、生产清单、wheel smoke 与准确性台账同步区分“官方 SDK 证据”和“真实 stdio/HTTP transport 证据”。明确 memory control 不执行 pipe/TCP/HTTP、远程/跨厂商、conformance、认证、授权/审批或生产日志；SDK/schema-valid 不建立业务授权。
- MCP 2025-11-25 official-SDK real-stdio control：官方 `mcp==1.29.0` 的 `stdio_client` 启动独立 Python subprocess，子进程以官方 `stdio_server` 将 low-level fixture 接到真实 OS stdin/stdout pipe；同次运行完成 initialize/ping/discovery、成功/schema-invalid/unknown-tool 调用与 graceful EOF。exclusive-create canonical receipt 只把内部 PID 用于 distinct-process 判断，公开 projection 固定 handler events `fixture.add, fixture.missing`、计数与完成状态，不发布 PID/path/raw transcript/raw 参数/result/error content，只保留 allowlisted sum。11 个测试覆盖真实运行、闭合报告与协同 rehash、duplicate/nonfinite/noncanonical/oversize receipt、no-overwrite 和项目 CLI；wheel smoke、教材、Lab、项目、面试、简历、生产清单与准确性门禁同步接入。明确未注入畸形 raw framing/stdout 污染，未触发 forced shutdown/cancel/deadline，也无 HTTP/SSE、TLS/OAuth、远程/跨厂商、conformance、业务授权或生产 supervisor 证据；本地 receipt 与无密钥 hash 不认证来源或执行。
- MCP 2025-11-25 official-SDK Streamable HTTP control：固定 `mcp==1.29.0`，以官方 `streamable_http_client`/`ClientSession` 连接独立 Python subprocess 中的 low-level `Server`、`StreamableHTTPSessionManager` 与 SDK ASGI adapter，在真实 IPv4 loopback TCP/HTTP 上执行 stateful initialize/ping/discovery、成功/schema-invalid/unknown-tool、POST/GET SSE 与 client-close DELETE。固定 profile 为 7 POST + 1 GET + 1 DELETE、8×200 + 1×202、7×SSE + 2×JSON；临时 canonical receipt 记录 `fixture.add, fixture.missing`、manager graceful shutdown 与私有 control shutdown，不发布 PID/session id/token/header/raw payload/参数/result/error。11 个测试覆盖真实运行、私有 control 缺 token→401、闭合报告、协同 rehash、unsafe receipt、no-overwrite 与项目 CLI；wheel smoke、教材、Lab、项目、面试、简历、生产清单和准确性门禁同步接入。私有 readiness/shutdown token 不是 MCP auth；未执行 MCP endpoint 的 malformed body、Host/Origin failure、网络故障、cancel/deadline、resumption、TLS/OAuth、远程/跨厂商、conformance、多 worker、业务授权或生产 supervisor，loopback port 先选后 bind 仍有竞争窗口。
- Guarded real-weight Qwen RAG runtime：新增与 attempt-1 不同的 case/query，让 fail-closed publication policy 真实包裹固定 Qwen2.5-0.5B-Instruct 的 callback。冻结 manifest `sha256:9ead4c06…40778a` 与 CPU FP32 report `sha256:00706d00…f29ede`：有证据 case 的 `GenerationMixin.generate` API invocation=1，模型生成“无权文档不得进行排序”后因漏引 reject；空授权证据 case 的 callback/framework invocation=0，在生成前 abstain；publish=0，public projection 不含 raw/finding。closed-schema verifier 重建 authorization-first BM25、context/packing relation、policy/public projection、count/hash/scope；33 个测试覆盖真实 tiny Transformers spy、run-loader options、runtime/workload 与 reviewed retrieval/packing preflight、真实报告、顶层身份和协同 rehash/strict JSON 负例，公共 API、wheel smoke、accuracy gate、教材/Lab/面试/简历同步接入。明确 method invocation 不等于内部 forward/kernel 或 provider request/billing/cancellation，offline verifier 不重放生成/token IDs/decode；两个共享 authored corpus/checkpoint 的 query 不证明语义蕴含、总体质量、GPU/vLLM、性能或生产集成。
- Fail-closed RAG publication policy：新增显式 `pre_generation/post_generation` 与 `publish/abstain/reject` decision；授权 context 为空时 generator spy 保持 0 calls，非空时只观察一次 raw output，citation short-ID/段落语法失败即拒绝发布，且 `semantic_entailment_verified` 永远为 false。审计 `to_dict()` 保留 raw/finding，allowlist `to_public_dict()` 明确排除二者，防止服务直接序列化内部 decision 绕过 reject。对先经严格 verifier 确认的 Qwen attempt-1 做 deterministic counterfactual replay：answerable 漏引为 1 call 后 reject，empty-evidence 为 0 call abstain，publish=0；冻结 replay `sha256:ed4d16ad…b13239`，精确绑定 source report/case/packing/raw-output identity。17 个测试覆盖 action、public projection、配置、错误、strict JSON、协同 rehash、完整重建和 CLI，wheel smoke 也验证安装后模块。明确 replay 没有观察 guard 当时包裹 Qwen runtime，不证明真实调用节省、语义蕴含、来源认证、总体安全或生产集成。
- Real-weight Qwen RAG failure control：复用固定 Qwen2.5-0.5B-Instruct revision 与 999,586,347-byte 执行 snapshot，在加载前重哈希，真实串起 authorization-first BM25、tenant/principal 高相关禁区、目标 tokenizer 完整 prompt packing、逐步 greedy logits/KV cache 和 `GenerationMixin.generate()`。冻结 manifest `sha256:4ee16617…09dfd` 与 recorded report `sha256:829663e2…e5b60`；attempt-1 不调 prompt、不 repair output，有证据 case 复述正确但漏引，无证据 case 在空 context 下编造 Kubernetes 步骤并耗尽 64-token cap，citation/exact-abstention gate 为 0/2。closed-schema verifier 重算 checkpoint/corpus/retrieval/packing/token/raw-output/local-audit/case/report identity，并覆盖 tiny Transformers 真执行、nested schema、cooperative rehash 与 CLI。明确真实执行与质量通过是两回事；两个 case 和语法 verifier 不证明语义蕴含、总体质量、来源认证、GPU/vLLM、性能或生产安全，verify→loader reopen TOCTOU 仍存在。
- Real Qwen checkpoint execution control：固定 `Qwen/Qwen2.5-0.5B-Instruct` revision `7ae5576…9a775` 的 7 个执行文件、999,586,347 bytes 与逐文件 SHA-256，其中 `model.safetensors` 为 988,097,824 bytes、SHA-256 `fdf756fa…b7fe`。控制在加载前重哈希全部选定 bytes，以 `trust_remote_code=False` 从已验证本地 snapshot 路径加载 `Qwen2ForCausalLM`/tokenizer，冻结 494,032,768 个参数并在 CPU FP32 eager 下真实执行 chat-template、prefill、KV-cache 第二步、full recompute 与 `generate()`。2026-08-13 录制报告固定 31-token prompt、`[1,31,151936]` logits、continuation `[17,151645]`/`2<|im_end|>`、cached/full max-abs error `3.719329833984375e-05 ≤ 1e-4`，self-fingerprint `sha256:56528a3e…dba62`。v1 manifest 只允许恰好两个生成 token；report verifier 对 source/artifact/runtime/model/tokenizer/execution/scope 使用 closed schema，并在攻击者协同重算无密钥 self-hash 后仍拒绝嵌套字段、runtime、冻结状态、shape、FP32 storage、误差门槛与范围漂移。22 个定向测试覆盖 manifest/snapshot、tiny Transformers 执行、tamper、strict JSON 与 CLI。明确 verify 后由 loader 重新按路径打开仍存在 TOCTOU；无密钥 hash 不认证发布者，单 prompt CPU run 不证明训练、质量、有效上下文、许可、安全、CUDA/vLLM、显存峰值或性能。
- Immutable model-release evidence control：固定 Meta Llama 3.2 官方 model card、Qwen2.5-0.5B-Instruct config 与 DeepSeek-V3 config 的不可变 commit URL、原始 byte length/SHA-256 和本地 strict semantic snapshot。Llama 的 1B/3B/128k/GQA 等只作为 vendor-reported fragments；Qwen 的 24-layer 14/2-head GQA 在 32,768 tokens、batch 1、2-byte element 下复算理想 K/V payload 402,653,184 bytes；DeepSeek-V3 同时出现 KV-head 与 MLA/MoE markers 时必须拒绝标准公式。可选 `--verify-upstream` 真实下载三份固定 artifact 并核对 bytes/JSON/fragments；13 个测试覆盖 snapshot/contract/scenario、tamper、path traversal、duplicate JSON、URL allowlist 与 CLI。明确不下载/运行权重、tokenizer/remote code，不证明 config-weight 匹配、发布者签名、有效上下文、参数量、质量、许可、runtime/GPU、显存或性能。
- A2A 1.0 official-SDK loopback control：固定官方 `a2a-sdk==1.1.2`，用 server subprocess 与 official client 在真实 IPv4 loopback TCP/HTTP 上解析 well-known Agent Card，选择 `JSONRPC`/`1.0` interface，执行 `SendMessage`/`GetTask`，并把远端 completed 与本地 artifact verifier 分开。raw HTTP 负例锁定 legacy `kind` → `-32602`、`A2A-Version: 9.9` → `-32009`；可选联网路径下载 SHA-256 固定的官方 v1.0.0 JSON Schema，以 Draft 2020-12 验证 Agent Card、Send Message Request 与 Task。11 个测试覆盖 strict JSON、ProtoJSON、schema bundle/ref、真实 client/server、subprocess purity、脱敏报告和 CLI；明确没有 TCK/完整 conformance、SSE、REST/gRPC、TLS、认证、签名 card、授权/审批、远程或跨厂商证据。
- MCP 2025-11-25 stdio lifecycle control：新增真实本地 client-launched server subprocess 与 OS pipe，严格执行 LF-delimited UTF-8 JSON-RPC、initialize/initialized、tools/list、三类 tools/call 和 stdout-only-protocol；同一 closed Draft 2020-12 contract 发布/执行 input/output schema，分开 tool execution error 与 `-32602` protocol error，并以 12 个测试覆盖 framing、duplicate/nonfinite/invalid UTF-8/byte cap、版本 fallback、生命周期、结构化结果、脱敏错误和 deterministic allowlist-projection fingerprint。公开报告不发布 raw messages，投影也不绑定参数/result content；明确未使用官方 SDK/完整 conformance suite、Streamable HTTP、网络认证、授权/审批、A2A 或跨厂商互操作，无密钥 fingerprint 也不认证来源。
- MCP 2025-11-25 Streamable HTTP loopback control：新增真实 `127.0.0.1` TCP/HTTP server subprocess 与单 `/mcp` endpoint，执行 Origin allowlist、随机 Bearer header gate、initialize/session/version、POST JSON/SSE、GET SSE、空 202 notification、并发 `notifications/cancelled` 和 DELETE→404。取消后的 stream 不发送 JSON-RPC response；公开报告不含 token/session/event id/raw HTTP/参数/result，固定投影指纹 `sha256:5a5cc3be…c8915`。10 个定向测试覆盖 strict finite JSON、in-process ASGI header/body/session 负例、SSE helper、lifecycle/cancellation timeout、unsafe server config、真实 loopback 与 CLI；明确 Bearer gate 不是 OAuth/业务授权，且无官方 MCP SDK/完整 conformance、TLS、远程、SSE 恢复、跨 stream non-broadcast 或跨厂商证据。
- Career navigation correction：岗位路线页不再把已存在的面试题库写成未来计划；新增岗位名称不统一的边界、四条主线的最小证据、主线+相邻能力选择法、当前面试/系统设计/简历材料入口与简历数字反向审计。CI 的 full extras 同步显式安装 `agents`，不再依赖 `evaluation` 偶然带入相同的 `jsonschema` 依赖。
- Recent paper review track：新增独立“论文解读”板块与 2026-08-12 固定快照，以 Hugging Face 2026-W32 周榜和 2026-07 月榜解释六篇入选，再加入两篇课程相关编辑精选；八篇均按固定 arXiv 版本拆解问题、方法、证据、限制、工程迁移和不可外推项。来源台账明确热度不等于质量、预印本/作者报告不等于同行评审或独立复现，并保留 RST、LongHorizon、Orca、ABot 与 opaque reasoning 的数字、精度、版本和时效冲突。导航、首页、参考资料、21 项来源 registry、论文边界检查、built-site freshness token 与回归测试同步接入。
- Opaque reasoning artifact security track：新增 provider-neutral AES-256-GCM 教学协议，对照 content-only 与 context-bound associated data，绑定 subject/tenant/session/branch/predecessor/model audience/issued/expiry，并分离 nonce 与 single-consumption ledger。16-case CLI matrix 固定展示四个弱协议跨上下文接受，以及 scope drift、retired key、claims tamper 和第二次消费的 fail-closed 行为。新增 strict JSON/JSONL trajectory release gate，只允许 text/tool-call/tool-result/citation publication projection，默认拒绝 reasoning/thinking/signature/encrypted、嵌套禁用字段、unknown block 和 schema drift，报告不回显输入名称/值；明确它不解析真实 provider block、不访问模型/网络、不证明当前 API 漏洞、生产 key custody/分布式 replay，也没有执行 secret/PII 扫描。教材、Lab 0D、Cloud API/Agent/安全/治理/生产清单、论文主来源与本地学习拓扑同步接入，并保留论文截至 2026 年 8 月原攻击已不可复现的披露后边界。
- Documentation presentation overhaul：新增全局中文文档样式，扩大桌面三栏可用宽度并限制普通段落行长，强化标题、列表、表格和导航层级；知识地图将长篇实现证据摘要改为按需展开，使主题地图回到首屏；工程项目矩阵在桌面完整展示，移动端转换为带“项目目录/当前能力/可运行入口/等级与证据”标签的纵向字段块，避免整页横向溢出。
- Introductory navigation links：首页新增按目标组织的快速入口；如何使用、学习路径、知识地图、仓库地图、环境矩阵、实验与项目索引增加上下文导航和下一步链接。学习路径中的每周主题、工程层级与数学补给改为可点击课程路线，使读者无需依赖侧栏即可从介绍页进入对应教材、实验、项目、检查表和准确性台账。
- Agent interoperability chapter：新增 MCP/A2A 与内部契约分层，区分 provider API、tool/context protocol 和 Agent-to-Agent protocol；覆盖 discovery/capability、Agent Card、task/message/artifact、版本策略、提示注入、授权、超时未知结果、幂等与 verifier 边界。工程学习路径、Agent 总览、MkDocs 导航、官方资料、准确性台账和实验 6A 同步接入；后续 A2A official-SDK loopback、MCP official-SDK memory 与自写 MCP stdio/HTTP controls 已分别补齐 SDK 或本地 transport 局部证据，但 MCP 仍无完整 conformance/远程认证，A2A 也无 TCK、远程或跨厂商证据。
- Versioned executable tool schema：新增 `JSONSchemaToolContract`，以同一 immutable schema 生成 Planner catalog 与 runtime Tool；显式固定 Draft 2020-12、closed root object、local-only `$ref/$dynamicRef`、无 `$id`/external retrieval、schema/instance byte cap、format annotation/enforcement mode，以及绑定精确 `jsonschema` 版本/模式/instance cap 的 contract identity。错误只输出 keyword 与 JSON Pointer，不回显 rejected value；不 coercion、不应用 default，也不替代 resolver/policy/approval/业务验证。16 个测试覆盖 schema/meta-schema、local ref、external ref、format、freeze、identity、size/redaction 与 schema-before-resolver/policy/handler；recorded planner control 改为同源 schema，并新增 parser 可接受但 runtime `const` 在 resolver 前拒绝的第四条负例。`agents` extra 显式声明 `jsonschema>=4.23,<5`。
- Strict JSON model planner boundary：新增 provider-neutral request/response contract、prompt/state/budget/tool/output-cap/model-revision fingerprint、mandatory provider request id/usage/cost/finish reason、deep-frozen tool schema，以及只接受 `tool/finish/escalate` 的 duplicate/nonfinite/fence/unknown-field fail-closed parser。Accepted decision 同时绑定 request、完整 normalized response/raw text 与 typed action，reported output usage 不得超过 cap；runtime policy/validator/approval/verifier 仍是权威边界。离线 recorded-response control 锁定 2 request + 2 response + 2 decision identity，真实运行一次只读 handler 和 verifier completion，并覆盖 request drift、fenced JSON、runtime schema rejection、missing capability 四条反例；41 个 parser/identity/metadata 单测与 4 个 control 测试通过。明确 recorded provider id、62 tokens、0.03 cost 都是 authored metadata，无网络/目标模型/API/账单/生产 IAM/开放语义证据，无密钥 hash 不认证来源或安全。
- Persistent extractive RAG ASGI service：新增 FastAPI closed-schema `/v1/rag/query`、body 外 `AuthResolver`、server request id、liveness/readiness、每请求 SQLite reopen、ACL-visible load→BM25→exact-span artifact、`no-store` response，以及稳定 401/422/500/503/504 错误。单进程 bounded semaphore 分开 queue/execution deadline；`to_thread` 超时或 client cancellation 后以 shield 保留 background task，并直到真实 work 结束才释放 permit，避免 504 后突破并发上限。演示 Uvicorn 入口只从环境变量取 static token、默认拒绝 non-loopback；ASGI control 固定 engineering/anonymous source 为 2/1、tenant injection=422、missing auth=401 和两个 artifact fingerprint。13 个定向测试覆盖 auth/ACL/strict body、空租户拒答、readiness、disabled docs、limit、queue/timeout、background permit 与异常脱敏。当前 CI 固定 FastAPI 0.115.6–0.115.x + Starlette 0.40–0.41 + Uvicorn 0.52.1；明确 ASGITransport 不证明 TCP/TLS/proxy/JWT/IAM，static bearer/per-process semaphore 不是 production auth/global admission，也无 cache、learned retrieval/LLM、容量或 SLO 证据。
- LangChain/LlamaIndex RAG parity control：canonical BM25 在 scorer 前执行 tenant/principal ACL，再分别通过真实 LangChain `BaseRetriever.invoke()` 与 LlamaIndex `BaseRetriever.retrieve()` 返回框架对象；adapter 新增 ACL metadata、连续 rank/duplicate ID/finite score gate、protected-key collision 拒绝、LlamaIndex 默认 embed/LLM metadata exclusion，以及相对同次 canonical results 的逐字段 strict round-trip。四文档 authored fixture 固定 engineering/anonymous 可见集为 2/1，跨租户与 finance-only 文档不可见，两种 PromptTemplate SHA-256、deterministic extractive artifact、Recall@4/nDCG@4 exact 对齐。明确这不证明框架默认 ACL、supplied canonical result 来源、learned embedding/vector index/reranker、provider/local LLM、网络、质量、延迟、扩展性或生产安全。
- Deterministic Transformers generation runtime control：在随机 3,824-parameter tiny GPT-2 上真实执行 forward、LogitsProcessor 与 GenerationMixin stopping loop，同时用 authored processor 覆盖全部 next-token scores。三条 exact trace 固定为 config EOS `{2,3}` 下 `[4,3]`、call EOS=5 覆盖后 `[3,5]`、call `max_new_tokens=2` 下无 EOS 的 `[4,6]`；另验证 caller GenerationConfig 未 mutation、重复运行 deterministic、非法 plan 拒绝和 CLI。明确框架返回没有 provider-style finish reason，报告仅由已知 plan/条件推断；不执行真实 tokenizer/chat template、公开 checkpoint、vLLM/provider 或 GPU，权重不决定 token，也不证明正常生成质量、延迟或吞吐。
- Generation-protocol three-way inspector：新增 strict combined JSON、canonical contract identity 与 tokenizer/model config/generation config 的 BOS/EOS/PAD/decoder-start set 对账，逐对报告 exact/subset/superset/overlap/disjoint，并标出 tokenizer/model vocab 越界。两份明确 authored 的 fixture 固定 EOS `{2}`→`{2,3}` 合法 review signal，以及 BOS/EOS/PAD 全部 disjoint、PAD 9 越过 8-token 上界的 drift 反例；另覆盖 generation config 缺失、PAD=EOS neutral observation、双 length field、duplicate/negative/bool ID、strict schema/UTF-8 与 CLI。`inspect_checkpoint.py` 现在尝试读取 GenerationConfig；`OSError` 只报告 `unavailable_or_load_error`，不冒充“文件不存在”。明确 normalized snapshots 不是 raw JSON hash，静态对账不解析 `generate()` kwargs、fallback/precedence、stop-string tokenization，也不证明 Transformers/vLLM/provider 实际停止、权重、来源、质量或许可。
- Strict decoder config inspector：新增 strict local JSON loader、canonical config fingerprint 与保守的 core/MoE/MLA marker ledger；仅在标准 attention 字段显式、自洽且无已知 MLA marker 时计算理想 dense K/V payload。三份明确以 `authored` 命名的 fixture 锁定 GQA 4096-token 536,870,912-byte、MoE-GQA batch-2 402,653,184-byte 和 MLA fail-closed 分支；不估 exact/active parameters，也不把 `max_position_embeddings` 当有效上下文。测试覆盖 MHA/MQA/GQA 分类、duplicate/nonfinite/non-object/非法 UTF-8 JSON、standard/MLA CLI round trip。`inspect_checkpoint.py` 复用该 contract，Base tokenizer 无模板时合法返回 null，Instruct token IDs 直接由 `apply_chat_template(tokenize=True)` 生成；mock 测试锁定 revision/`trust_remote_code=False`、resolved config metadata 和非法 token list 拒绝。真实 checkpoint fingerprint 明确来自可能含库默认值/metadata 的 normalized `AutoConfig.to_dict()` snapshot，不冒充 raw `config.json` byte hash。边界明确不检查/认证权重、remote code、tokenizer/generation config 来源、有效 runtime/quant layout、质量或许可，三个 fixture 不冒充 Llama/Qwen/DeepSeek 规格。
- Deterministic comparison HTML report：新增 `render-comparison-html`，从严格 comparison v2 派生自包含中文报告，展示 pass/fail、run/case identity、总体与 protected-slice 区间、case/cluster resampling ledger、gate 配置/原因、metric revisions 与证据边界。所有动态文本统一 HTML escape，页面无 JavaScript/外部资源并带 restrictive CSP；测试覆盖真实 fixture deterministic render、cluster exact 字段、闭合 cell+`script src` payload 与 CLI scope receipt。明确 `artifact_only_render` 不重开 evidence graph、不重算统计、不认证 artifact，HTML 可覆盖且不是 canonical decision artifact；尚未提供跨浏览器、打印或辅助技术人工视觉回归证据。
- Full local evaluation evidence verifier：新增 `about-llm-eval verify-evidence`，严格重开 cases、baseline/candidate answers/results/run manifests 与 comparison；重算 ordered answer/case identity，要求 scorer/metric revision 与当前实现精确一致，重新评分，并从 artifact 的 resampling、cluster、bootstrap、gate/slice 配置重建最终 comparison。测试覆盖真实 fixture round trip、answer drift、manifest fingerprint 已同步更新但 score 与 answer 不符，以及内部自洽但不对应 results 的 comparison summary；机器 scope 明确不认证 artifact/`system_id`、不重放模型/provider，也不验证 sampling/cluster 假设、construct validity 或 production impact。
- Authenticated evaluation release-ledger control：新增 domain-separated HMAC-SHA256 连续链，绑定 release/artifact identity、原始文件 size/SHA-256、decision、caller timestamp、key id 与前序 MAC，支持逐记录 key rotation；strict canonical loader 拒绝 duplicate/unknown/nonfinite、乱序/断链/重复 identity，snapshot writer exclusive-create + file-fsync。三记录公开 fixture 重哈希 baseline/candidate manifests 与 comparison，测试覆盖字段篡改、wrong/missing key、reorder、artifact drift、no-overwrite，以及“无 trusted head 时合法前缀仍通过、有外部 head 时拒绝截断”。机器 scope 分开报告 authenticated chain、artifact rehash 与 trusted-head match，并明确公开 key/HMAC 不证明 key custody、不可否认性、真实时间/执行、目录原子发布或 verify-load TOCTOU 安全。
- KV-aware recompute-preemption batching oracle：把 continuous-batching boundary 与 `PagedKVAllocator` 接成同一 deterministic CPU state machine；每个 causal forward position 真实占用 metadata block，容量不足时只能抢占当轮尚未工作且 stable-FCFS 优先级更低的 youngest resident，避免双向驱逐；释放全部 KV 后按 FCFS 重新 admission，先重建旧 context，再允许新 logical progress。固定 3×2-token block、两请求 fixture 逐轮 work 为 `3,3,1,1,2,1`：逻辑 work 9、重计算 2、实际执行 11，B 只在 boundary 2/6 发两个 token而不因 rebuild 重复输出，peak/最终 block 为 3/0。另以 repeated-state fail-closed guard 和随机小状态审计约束终止性。明确这是无 K/V 数值、无 swap/prefix reuse/CUDA/墙钟的 authored policy，不等价于任何 vLLM release，也不证明 VRAM、吞吐、延迟或质量。
- Strict PEFT deployment export control：随机 tiny GPT-2 CPU LoRA 在 frozen base exact 不变的前提下保存/reload adapter、执行 `safe_merge`、保存/reload merged full weights，并加入可重载 WordLevel tokenizer/special tokens/chat template；8-step fixture 固定为 28,032 total/1,024 trainable、4 adapter tensors、110,632/4,608/110,632-byte 三个 weight 文件，adapter/merged reload error=0、merge error≈$8.94\times10^{-8}$、template IDs `[5,7,2,9,2]`。Strict canonical manifest 覆盖完整 13-file/236,589-byte payload，绑定 identity、目录 contract、path-sorted size/SHA-256 与 descriptor-set digest；19 个 verifier 测试还要求三个 safetensors 可解析、base/merged 完整 config payload 与 tensor key/dtype/shape signature 一致、每个 target module 同时存在 LoRA A/B tensor，并拒绝 extra/missing/symlink/traversal、duplicate/non-canonical/unknown manifest、资源超限、内容篡改与协同 rehash 后的 weight/config/adapter/tokenizer 语义漂移。明确结构一致不证明权重数值正确；PEFT 自身不强制仓库 manifest，identity/unkeyed hash 不认证来源，且无训练恢复、量化 merge、目标 checkpoint/CUDA、质量/runtime/license、目录原子发布或 verify-load TOCTOU 防护证据。
- Strict MiniGPT training checkpoint/exact-resume control：新增 pickle-free `about-llm.minigpt-training-checkpoint.v1`，保存 FP32 MiniGPT 全参数、每参数 AdamW step/一阶/二阶矩、线性 LR/global step、Byte-BPE/config/tied contract、数据 shape+content fingerprint、shuffle permutation/cursor/epoch、data-generator RNG 与 dropout Torch CPU RNG，并只允许 zero-grad optimizer boundary。固定 7×5 token、batch 2、dropout 0.2、6-update fixture 在第 3 步切分后，与不中断路径的 batch/epoch/LR/loss及最终 model/optimizer/stream/RNG bit-exact；artifact 为 manifest 11,341、payload 42,520、51 tensors、总计 53,917 bytes，loader 不污染外部 Torch RNG。10 个测试另拒绝数据漂移、梯度未清、optimizer/LR/manifest/tensor/permutation/RNG 篡改、trailing/truncation、资源超限与覆盖旧文件。范围仅为当前 CPU FP32 MiniGPT、单 AdamW group、线性 schedule；不嵌入数据，不保存未使用的 Python/NumPy/CUDA RNG，也无 AMP/累积/worker/distributed/目标 checkpoint、质量、来源认证或断电原子发布证据。
- Repo-native quantized MiniGPT inference checkpoint：保存实际 Byte-BPE merge payload、canonical GPT config、architecture revision、16 个唯一量化矩阵/FP32 vector 参数与 tied LM-head contract，并重建 causal mask、恢复真实 PyTorch forward。默认 fixture 固定为 FP32 parameters 10,976 bytes、manifest 3,904、payload 4,760、artifact 8,720，`abc abc`→`[257,32,257]`、logits `[1,3,258]`、重复 load exact、FP32-logit RMSE≈0.00477277。Loader 在模型构造前校验资源上限、strict manifest 和完整参数语义；21 个测试另覆盖 bias-free two-layer/5-bit/non-divisible group、runtime/config/mask drift，以及协同重算 outer hash 后的 tokenizer/name/shape/offset/inner/NaN 漂移、no-overwrite 与 handle release。明确“完整”只对 trusted repo MiniGPT inference revision 成立；不嵌入 forward source，也无 normalizer/special/chat template、训练状态、通用格式/runtime、low-bit kernel、目标质量、VRAM、速度或来源认证证据。
- Strict multi-matrix quantized bundle reference：用 24-byte v1 header、canonical JSON manifest、连续的完整单矩阵 artifacts 和 outer SHA-256，绑定 model/tokenizer revision、architecture identity 与 name-sorted tensor offset/length/digest；loader 拒绝 duplicate/unknown/non-canonical manifest、name/order/offset/digest 漂移、trailing/truncation 和资源上限外输入。默认 two-layer NumPy fixture 固定得到 FP32 288 bytes、raw quantized payload 124 bytes、inner artifacts 252 bytes、bundle 987 bytes、output RMSE≈0.0368725，并验证 byte/quantized-forward exact reload。明确 tokenizer 只有 identity，未保存未量化 state、model forward、shard/runtime layout 或 fused kernel；unkeyed hash 不认证来源，exclusive-create + file fsync 也不证明 crash/断电原子发布，因此不冒充完整 LLM checkpoint、GGUF/safetensors、质量、VRAM 或加速证据。
- Evaluation comparison v2 cluster integration：`compare` 可从 case metadata 读取 cluster id，显式选择 case-weighted ratio 或 equal-cluster estimand，并对 overall/protected slice 运行 exact `G^G` 或 seeded Monte Carlo bootstrap；artifact 绑定 unit/key/weighting/exact threshold，逐 result 保存 cluster sizes、estimand、method、实际 resample 数、quantile 与 seed。Loader 复核 sizes、difference、exact fraction、配置和 gate 决策；明确安全差值跟随同一 weighting、延迟仍为逐 case mean，且 artifact 自洽不建立 cluster 正确性、独立/代表性或 coverage。v2 不静默加载旧 v1。
- Deterministic MinHash/LSH lexical candidate reference：以 stable SHA-256 shingle mapping、seed-derived affine hashes、完整 band tuple 生成候选，绑定 item/report identity 并逐候选 exact Jaccard recheck；提供 ideal `1-(1-s^r)^b` 曲线和 exhaustive snapshot recall audit。默认 5-item/10-pair toy 得到 3 candidates、1 true positive、2 false positives、recall=1/precision=1/3；另锁定 1-hash 漏掉 Jaccard=2/3 pair 的 recall=0 反例。明确 exhaustive audit 仍为 O(N²)、snapshot 不外推、readiness 未改用近似 pass，也不证明 semantic/translation duplicate、真实阈值或 hash authenticity。
- Budgeted cloud HTTP single-attempt reference：在 target preflight 后 reserve，将完整 usage 成功结算、明确 Pool/Connect 前失败 cancel、HTTP error/2xx 缺 usage或 malformed/task cancellation uncertain 接入内存与 SQLite ledger；离线 MockTransport demo 固定得到 settled 66 + uncertain 80 = committed 146 micro-USD。Wrapper 强制 `max_attempts=1`，要求每个 replay 使用独立 reservation，避免一次逻辑-call cap 掩盖多次可能计费调用；明确不执行网络、不解析 provider error usage，也不消除 provider effect—SQLite commit crash window。
- Budgeted cloud JSON retry orchestrator：为原 HTTP executor 增加默认关闭的 attempt lifecycle hooks，保留 bounded retry、有效 `Retry-After`、monotonic deadline、replay-safe 与 outcome-uncertain 语义；新 wrapper 在每次发送前以唯一 attempt id 独立 reserve，并在 sleep/下一次发送前先 cancel/uncertain，最终完整 usage 才 settle。离线 SQLite demo 固定 500→200 为 uncertain 80 + settled 66 = committed 146；测试另覆盖 Connect→200、hard limit=140 时第二次 reserve 在 transport 前失败且网络仅 1 call、429 delay、取消、malformed/缺 usage与重开 event 顺序。明确只覆盖 JSON/MockTransport/local quota，不支持 streaming partial-output retry，不解析 provider error usage，也不证明真实 usage/invoice、server cancellation、idempotency、exactly-once billing 或跨系统原子性。
- SQLite durable cloud usage-budget ledger：把 pricing+limits singleton config、request-bound reservation/tombstone 与 event timeline 持久化，使用 `BEGIN IMMEDIATE` 防止同一文件的并发 writer 重复占用 capacity；测试覆盖 crash/reopen active 保留、settle/refund、uncertain、post-call overrun 持久化、多连接竞争、配置漂移、event 回滚、文件句柄与整数边界，并提供拒绝覆盖旧库的离线 demo。明确 active 不按 TTL 自动释放，无密钥 fingerprint 不抗协同篡改；local SQLite 不与 provider call 原子，也不证明 usage/invoice、取消、断电 durability、分布式 quota 或 exactly-once billing。
- Cloud usage-budget reservation reference：从三类 RequestSpec 机械提取唯一 output cap，并以 credential-redacted canonical fingerprint 绑定 billing scope、URL、body/header 与 reservation；同 scope key rotation 稳定，prompt/cap/scope drift 改变 identity。另绑定 caller-reviewed provider/model/revision/checked_at pricing snapshot，以整数 micro-USD per million tokens 做线程安全预留，覆盖 settle/refund、确定未发送 cancel、unknown usage 保守入账与 actual-over-reservation 的 post-call breach。固定 `$1/M + $2/M` fixture 精确得到 60+10 预留 80 micro-USD、58+4 结算 66 micro-USD；明确 hash/内存锁/authored price 不认证 caller/transport，不证明保密、目标 tokenizer、provider hidden/cache/reasoning token、发票、HTTP 原子性、持久/分布式 quota 或 billing reconciliation。
- Token-aware finite-language constrained-decoding oracle：把 authored literal set 编译成 Unicode-code-point trie，对每个 supplied token fragment 执行完整多字符转移，EOS 只在 accepting state 开放，mask 后按 allowed raw mass 重归一化且 zero-mass typed failure。固定 JSON-like 反例屏蔽首字符合法但完整非法、概率 0.65 的 `1]`，将 `1}`/`2}` 的 0.25/0.10 归一化为 `5/7`/`2/7`；明确不执行真实 tokenizer byte state/normalization、JSON Schema/CFG、模型/KV/GPU 或证明语义与性能。
- Deterministic table-driven beam-search oracle：active prefix 按累计 log probability 与 token-id tie-break 剪枝，EOS 立即完成且不再展开，length cap 显式终止；最终分数固定为 `log_probability / generated_length**alpha`，生成长度含 EOS、不含 prompt。两个反例分别证明 beam 1 会剪掉概率 0.4 的更优路径而 beam 2 可保留，以及 alpha 0/2 会翻转长短候选；明确不模拟特定 runtime candidate cap/early stopping，也不执行模型、tokenizer、KV/GPU 或证明质量与性能。
- Clustered paired percentile-bootstrap oracle：整 cluster 有放回重采样，严格区分 case-weighted sampled-sum/sampled-size ratio 与 equal-cluster sampled means；固定两 cluster 的 4 个 ordered resample 得到 `[-0.875,0.975]`/3⁄4 和 `[-0.925,0.925]`/1⁄4，并以 \(3^3\) brute force、7-cluster exact cap、seeded MC 和分块内存测试锁定语义，明确 exact/percentile 不保证小 cluster coverage、BCa、独立代表性、无干扰、因果或模型改善。
- Cluster-joint paired randomization oracle：同一用户/文档 cluster 的 case 共享一个 sign，显式区分 case-weighted cluster-sum/总 case 与 equal-cluster mean/cluster 数 estimand；固定 5×`+1`/1×`-1` 反例将 naive 7/64 与 joint 2/4、equal-cluster observed 0 分账，并加入 24-unit exact cap 和 Monte Carlo sign-matrix 内存界，明确不建立 cluster/exchangeability/interference、预选 weighting、因果或模型改善。
- Sequential peeking exact oracle：对预设 `[10,20,30,40,50]` informative-pair looks，以 doubled-tail exact sign test 和 `(n, positive_count)` `Fraction` dynamic program 计算首次拒绝；逐次 0.05 的 null familywise error 精确为 `7109832616777/70368744177664 ≈ 0.1010367984`，事前 Bonferroni 0.01 split 为 `2142139082367/140737488355328 ≈ 0.0152208136`。小规模穷举复核 DP，且明确不枚举 `2^50`、不实现 confidence sequence，不建立真实 pair/cluster、抽样、effect/power、模型或线上因果证据。
- Holm step-down multiple-testing oracle：对预定义 p-value family 执行 stable rank、`m-i+1` multiplier、running maximum、cap 与 input-order remap；固定 `[0.04,0.01,0.03,0.20]` 得到 adjusted `[0.09,0.04,0.09,0.20]` 且 alpha 0.05 只拒绝原索引 1，并明确 FWER 控制依赖有效 component p-value、不修复 post-hoc family、反复窥视、cluster、effect importance 或因果。
- Paired randomization/sign-flip evaluation oracle：对非零 case differences exact 枚举或 seeded Monte Carlo，支持预指定 greater/less/two-sided、零差值分账与 plus-one p-value；固定 4×`+1`/1×`0` fixture 得到 greater `1/16`、two-sided `2/16`，并明确不证明 exchangeability、cluster independence、multiple-testing correction、effect importance、因果或模型改善。
- Incremental UTF-8 stop-string matcher：跨任意 byte chunks 严格解码，以 longest partial-prefix withholding 防止提前泄露 stop，固定 first-completion 与同字符配置顺序 overlap 语义；fixture 跨 emoji/`<END>` 分片并覆盖 atomic UTF-8 failure，同时明确客户端截断不证明 provider finish reason/usage、server cancellation、KV/GPU release 或停止计费。
- Deterministic next-token sampling NumPy oracle：固定 sign-aware repetition、temperature、exact top-k tie-break、post-top-k top-p crossing/renormalization 和 token-id-order inverse CDF；固定 fixture 精确得到 `[4/7,3/7,0,0]` 与 sampled token 1，并以 signed-logit fixture 区分 repetition 与 frequency penalty，明确不冒充 runtime 默认、跨 RNG replay、模型生成质量或性能证据。
- Security-scoped prefix-cache metadata oracle：完整绑定 trusted tenant/visibility domain、authorization/policy、model/tokenizer/chat-template/adapter、position/RoPE config、KV dtype 与 exact token ids，执行 longest-prefix full comparison、lease-pinned LRU 和全 leased 原子容量失败；固定 fingerprint collision 仍拒绝跨 tenant/错 token，并明确 hash 不负责授权/保密，toy 不包含真实 K/V、vLLM、VRAM、timing-channel mitigation 或性能证据。
- NumPy MoE routing/capacity oracle：padding-aware stable top-k、expert/token/rank deterministic tie-break、per-expert `ceil(factor*N*k/E)` capacity、assignment/token drop 双分母、drop 后可选重归一化、balance/z-loss/entropy 诊断，以及 kept assignment 的 bias-free linear expert dispatch/combine；固定 fixture 精确验证 counts `(3,4,1)→(2,2,1)` 与 3/8 assignment drop，并明确不冒充训练 router/MLP、DeepSeek/Qwen checkpoint、all-to-all/GPU、质量或性能证据。
- Continuous-batching 离散 CPU scheduler oracle：固定 FCFS admission、sequence/token cap、decode-first 与 chunked-prefill 进展语义，逐 boundary 记录 queue、prefill 首 token、decode/completion、active/elapsed capacity，并用三请求 fixture 严格验证 causal forward work 为 `sum(prompt+output-1)=10`、逐轮 slots 为 `3,3,2,2`；明确不执行模型/KV/GPU，不冒充 vLLM scheduler equivalence、wall-clock 性能、GPU utilization 或 SLO。
- Mask-aware GAE 与 PPO clipped-surrogate NumPy reference：显式区分 terminated/truncated 的 bootstrap 与 episode continuation，覆盖 padding、正负 advantage clipping、sampled KL proxy，并用 sampled ratio=1 但完整 categorical KL>10 的反例限定证据；只证明 CPU objective 数学，不冒充 PPO rollout/optimizer 或对齐效果。
- PyTorch 两状态 PPO control：真实执行 on-policy categorical rollout、冻结 old log-prob/snapshot、GAE、advantage normalization、clipped policy/value/entropy loss 与 96 次 minibatch optimizer step；用可枚举 expected return 验证从 1.0 提升到 1.8 以上，同时明确不含语言模型、RM、reference KL、截断环境、GPU 或生产稳定性证据。
- Tiny Transformer PPO control：随机 GPT‑2 backbone 真实执行 integer-token autoregressive rollout、冻结 behavior/reference、sampled reference log-ratio shaping、GAE 与 36 次 optimizer step，并以等价汇总 \(6^2\) 条 trajectory 的精确 reward 验证从 \(1/3\) 提升到 1.8 以上；不冒充自然语言、learned RM、目标模型、CUDA 或对齐质量证据。
- 本地 text PPO control：WordLevel tokenizer 与 chat template 真实贯通自然语言 prompt、两 token autoregressive rollout、EOS termination、generation-cap truncation、padding、分离 tiny actor/critic、冻结 behavior/reference、GAE 与 96 次 optimizer step；精确枚举均匀初始 expected reward \(25/169\) 和 `good, EOS` 概率 \(1/169\)，训练后门槛分别为 1.9 与 0.95。有限时域 objective 默认不在 cap 后 bootstrap，显式反事实会标记与 oracle 不一致；不冒充 learned RM、真实人类偏好、目标 checkpoint、CUDA、自然语言质量或生产稳定性证据。
- Learned-RM PPO proxy-exploitation control：用一个 authored pair 真实训练并冻结 tiny Transformer scalar RM，显式做训练-pair midpoint centering，并让 generation allowlist 贯穿采样、old/new distribution、entropy、KL 与精确枚举。完整 57-response support 上，RM train accuracy=1、margin=5.57，但 chosen 仅排第 38；六轮 PPO 将精确 proxy 从 2.739 提至 4.652，strict target success 从 \(1/64\) 降至 \(4.99\times10^{-4}\)，dense partial credit 则从 \(15/64\) 升至 0.566。该分账只作为 tiny authored support 上相对严格目标的受控反例，不冒充所有指标恶化、人类偏好、目标模型、CUDA 或生产实证。
- 面向开发者与算法工程师的新项目规格。
- Markdown/MkDocs、Notebook、可复用源码和生产项目四层结构。
- 模型谱系、环境矩阵、求职路线和实现验收矩阵。
- NumPy attention 正确性基线、PyTorch/JAX 微型 GPT 和可逆字节 tokenizer。
- NumPy 现代 decoder correctness oracle：RMSNorm、interleaved RoPE、GQA 与增量 KV Cache/full causal attention 等价测试；明确物理 K/V repeat、float64 reference 和小数组证据不代表生产 kernel。
- 确定性 byte-level BPE reference 与 CPU 实验：不跨 document 统计、稳定 tie-break、ranked non-overlapping merge、UTF-8 byte expansion/round-trip、非法 merge graph 测试，并明确不等于 checkpoint tokenizer。
- 三本经过执行验证的 Attention、MiniGPT 与 RAG Notebook。
- 原生 BM25、租户 ACL、RRF、Recall@k 与 MRR。
- 带审批、幂等、预算和冲突检测的 Agent 工具执行内核。
- PyTorch LoRA Linear、合并导出与冻结/等价性测试。
- 文档与代码双 CI、环境诊断和 Notebook 执行器。
- OpenAI-compatible SSE 压测、TTFT/TPOT/吞吐定义与 vLLM 单卡路线。
- JSONL 评测 runner、文本指标、配对 bootstrap 与发布门禁。
- Transformers 离线训练/生成 smoke test 与真实 checkpoint 检查。
- LangChain/LlamaIndex canonical RAG adapter 与框架等价测试。
- 面试题、系统设计题、简历项目和工程项目索引。
- Dense retrieval、sentence-transformers embedding 与 cross-encoder adapter。
- GPT/DeepSeek/Qwen、Claude、Gemini 三类云 API 契约与离线测试。
- PEFT tiny GPT LoRA smoke test 与 TRL 单卡 SFT 入口。
- SQLite Agent ledger、原子 claim、pending reconciliation 防重放。
- GPT、Llama、Qwen、DeepSeek、Claude、Gemini 独立模型章节。
- Markdown 结构切分、稳定 chunk id、增量 upsert/delete 与授权引用审计。
- Agent pending 调用的外部成功确认、放弃/补偿和不可重放审计流程。
- QLoRA 分项显存估算、OOM 降级顺序和 NF4 单卡训练入口。
- JSON Schema/引用指标、overall/切片汇总与 Markdown 评测报告。
- RAG 摄取、检索、引用、生产，Agent 架构/runtime/评测，SFT/QLoRA、推理/vLLM 与评测方法共十二篇重点进阶专章。
- 分级相关性的 nDCG@k 指标与全包 strict mypy 验证。
- 内容准确性政策、官方核验台账、模型/API 版本边界与自动化准确性检查。
- 可执行 KV Cache 容量公式、严格的 SSE token 计数和 text-only 云响应校验。
- 可运行的 Markdown RAG CLI：tenant/principal ACL、BM25、引用上下文、Recall/MRR 与样例 JSONL。
- 可运行的评测门禁 CLI：recorded-output scoring、切片报告、paired bootstrap、protected slice 与发布 exit code。
- 可运行的 Safe Agent 故障/恢复 CLI：稳定 proposal/execution fingerprint、失败预算、SQLite pending 调查与 reconciliation 审计。
- 可运行的云 API 离线契约 gate：三类 provider fixture、严格类型、请求快照和 credential redaction。
- Provider-neutral 云 API retry oracle 与离线 decision matrix：显式 status allowlist、attempt/deadline、bounded exponential backoff、注入式 jitter、严格 `Retry-After` 和 replay/outcome fail-closed；明确不证明真实 provider 的错误、幂等、配额或计费语义。
- Async cloud HTTP reference executor：caller-owned httpx client、exact origin/HTTPS/no-query/no-redirect、monotonic overall/attempt timeout、connect 与 outcome-uncertain 分类、取消传播、strict object JSON、response acceptance cap 和脱敏 attempt trace；MockTransport 证据不冒充真实 DNS/TLS/provider 可用性。
- Bounded SSE byte framer 与三类 text-only stream state：任意 UTF-8 chunk、BOM/CRLF/CR、多行 data、line/event/total gate、截断 EOF，以及 OpenAI-compatible `[DONE]`、Anthropic block/message、Gemini finishReason+EOF 的独立终止契约；不冒充 streaming HTTP 或非文本事件覆盖。
- Async SSE HTTP executor：pre-body status retry、严格 event-stream Content-Type、sequential update backpressure、idle/overall deadline、partial-body no-replay，以及所有成功/失败/取消路径的 response close；MockTransport close 不冒充真实服务端取消或计费停止。
- RAG SQLite schema-v1 chunk store：stable chunk 增量计划、expected-version optimistic concurrency、同版本 source/chunker-config fingerprint 漂移拒绝、显式 source delete、strict JSON reload、tenant/principal pre-scoring filter，以及 trigger 注入中途失败后的单库事务回滚；新增 `store-upsert/store-delete/store-retrieve` JSON CLI 与 duplicate/non-finite JSONL 拒绝，明确 lexical/单库事务证据不外推到 ANN 或跨存储原子性。
- RAG SQLite 灾备闭环：online backup、一律不覆盖输出、strict versioned manifest、物理 size/SHA-256、schema/foreign-key/row-level logical fingerprint、独立 verify 与恢复到新路径；测试覆盖快照后源库更新不改变 backup、物理/逻辑篡改和未版本化 trigger，明确 unsigned/unencrypted 单文件 fixture 不证明来源、断电 durability、RPO/RTO 或远端 vector store 恢复。
- MiniGPT residual activation patching reference：真实 PyTorch forward hook、aligned clean→corrupt position patch、raw logit difference、未裁剪 recovery、小分母 fail-closed，以及联合 causal-prefix/未来位置负对照；可运行脚本明确随机权重、batch=1 与 post-hoc metric 只证明 intervention plumbing，不冒充目标模型自然 circuit。
- Continual-learning accuracy-matrix reference 与两任务 replay control：解析 ACC/BWT/FWT/非负 peak-to-final forgetting、严格矩阵/baseline 输入契约、单遍 uniform reservoir，以及真实 CPU PyTorch 顺序 SGD 的 no/64-example/full replay 20-seed 配对 bootstrap；显式报告 optimizer-step matched 但样本呈现量不匹配、固定 task/data 不在区间覆盖范围，并拒绝外推到目标 LLM、安全 retention、隐私成本或通用 replay 结论。
- JAX/Optax MiniGPT 训练闭环：JIT train step、显式 optimizer state、梯度裁剪、同步计时与 CPU overfit 实验。
- JSON-only canonical artifact fingerprint 与 SHA-256 identity，明确只证明显式配置的序列化身份。
- 代码任务 pass@k 解析实现、严格的 `1 <= k <= n` 输入契约与单元测试。
- Typed conversation memory reference core：canonical JSON 快照、来源、TTL、profile consent、时序正确的 supersession/retraction、tenant/subject 隔离与解析测试。
- AI 产品设计独立教材，覆盖真实状态机、coverage-risk、evidence UX、外部动作确认、记忆控制、可访问性、实验和遥测边界。
- 可复制的治理工件模板：use-case inventory、impact assessment、数据/模型/系统卡、控制证据、供应商尽调、发布决定、exception、incident 与 retirement。
- Binary calibration 与 selective prediction：Brier score、equal-width ECE、tie-aware risk-coverage 的解析实现、17 个指标测试和离线 `calibrate` artifact CLI。
- 合成数据审计 reference core 与 CLI：lineage、required verifier、generator/verifier revision overlap、exact identity、generation round、human review 和 mixture repetition 诊断。
- 合成数据、蒸馏与反馈环独立教材及离线工程项目，覆盖 rejection sampling 重加权、self-training、soft/hard distillation、多代 collapse 实验和真实锚点边界。
- Inference attempt 与联合 SLO 分析：失败终态、成功 latency/throughput、HTTP failure 分类、离线 trace CLI、hash manifest 与解析测试。
- RAG source-level 评测升级：graded relevance、Precision/nDCG、all-evidence recall、显式 no-answer 分母、ACL/corpus miss 诊断和不完备 qrels 边界。
- RAG recorded-answer gate：严格 answer/abstain/error artifact、atomic claim、judgment provenance、授权 context 复查、引用/判断覆盖与保守 grounding 分母。
- RAG context packing reference：可注入完整 prompt cost、逐候选 ACL 复查、document/source 去重配额、prospective budget 计数和可审计选择原因；CLI byte fixture 明确不冒充 token。
- RAG authorization-first rerank reference：scorer 前 tenant/principal 二次过滤、唯一候选与连续 rank 契约、有限 score/稳定 tie-break、query/chunk/content/scorer identity trace，以及 strict recorded-score CLI；CrossEncoder adapter 复用同一核心，authored 分数不冒充 learned model 执行、质量、tokenizer/truncation 或性能证据。
- Agent 工具调用改用严格 JSON、递归不可变快照与 `sha256:` canonical fingerprint，避免审批后嵌套参数漂移及 ledger 参数明文；CLI artifact 拒绝重复 key/未知字段，SQLite result/reconciliation 也拒绝宽松 JSON；旧实验 ledger identity 不兼容。
- Typed Agent trajectory/effect gate：独立 task verifier 与安全 guardrail，显式 handler/effect/pending 事件、失败分母、proposal-step/handler-attempt 双预算、policy over-refusal、逐 case findings、策略/环境/verifier provenance，并拒绝用任务成功平均掉安全违规。
- Safe Agent 默认拒绝 policy control plane：可信 ExecutionContext/ResourceRef、同 tenant exact capability、indeterminate fail closed、cache replay 重新授权，以及绑定主体/资源/tool/policy revision 的 execution fingerprint 和过期 ApprovalGrant；明确 unsigned fixture 不等于生产 IAM/签名审批。
- Verifier-driven typed Agent loop：scripted planner、tool/finish/escalate decision、step/model-token/cost/monotonic-time budget、repeated action/`A/B/A/B`/repeated error 终止、approval pause 与 fail-closed extension error；离线 CLI fixture 明确 supplied usage/local exact verifier 不等于真实模型、计费或开放任务验证。
- Agent handler result 在返回边界执行严格 JSON snapshot 与递归冻结；caller 后续修改不影响 outcome/cache，非 JSON 结果失败并保持 pending，避免不确定副作用被重新执行。
- Safe Agent approval checkpoint/resume：严格 JSON round-trip、canonical fingerprint、identity/pending-decision/budget/usage/history 绑定、runtime handler counter 恢复、当前 policy 重新授权、过期 grant typed rejection，以及 SQLite CLI restart；明确无密钥 hash、非原子 checkpoint+ledger、active-time 口径与 unsigned fixture 边界。
- SQLite Agent ledger 的短连接现在在 commit/rollback 后显式关闭，避免 Windows checkpoint/restart 测试和运维文件操作被残留 handle 阻塞。
- Safe Agent transactional outbox reference：local task state + pending effect 单事务提交、唯一 identity、lease claim/renewal、retry/dead letter、严格 JSON/脱敏错误与事件时间线；离线 crash demo 重现 provider success 后 ack 前崩溃并用同一 effect id 重投，明确只证明 SQLite at-least-once delivery 与模拟 provider 幂等，不证明远端 exactly-once。
- Symmetric group-wise weight-only quantization CPU reference：显式 `[-qmax,qmax]` code、全零 group、FP32 scale、offset-binary LSB-first dense bit packing、canonical padding/unused-code 拒绝、strict v1 little-endian 单矩阵 artifact、unkeyed SHA-256、no-overwrite 写入、2–8 bit exact round-trip、反量化/linear-output error，以及 raw payload/serialized file/NumPy container 分账；仍不冒充完整模型 artifact、来源认证、GPTQ/AWQ、low-bit kernel、目标质量或加速证据。
- INT8 KV-cache quantization CPU reference：per-token/per-KV-head K/V 独立 absmax、`[-127,127]` code、全零 scale=1、保留 `-128`、code/scale/metadata 存储分账，以及 dequantized GQA causal probability/output error 与 incremental-prefix 等价测试；可运行 toy 不冒充 INT8/PagedAttention kernel、runtime resident VRAM、目标模型长上下文质量或加速证据。
- Exact speculative rejection-sampling probability oracle：解析验证 `min(1,p/q)` 接受、positive `(p-q)` residual、rejection=TV 与一步 target marginal 恒等式，并覆盖 block 首拒绝截断和全接受 bonus；离线 toy 明确 authored probability/Monte Carlo/CPU 控制流不等于真实模型 logits、KV/GPU kernel 或 speedup。
- Paged KV metadata allocator reference：固定 block partition、logical table、prefix refcount sharing、shared partial-tail COW、释放复用、logical/physical/fragmentation 分账，以及容量不足时 append mutation 前原子失败；toy 明确 occupancy 模拟不等于真实 KV tensor copy、PagedAttention GPU kernel、preemption、VRAM 或性能。
- Linear Bradley–Terry reward-model CPU control：NumPy 从零实现稳定 pairwise loss、strict accuracy/tie、full-batch gradient 与可选 L2；confounded fixture 重现 train accuracy=1 但 counterfactual held-out=0，平衡长度反事实 pair 后 length 权重约为 0。toy 明确 authored numeric features/preference 不等于 text/Transformer RM、人类标签、目标模型、reward hacking 或 policy optimization 证据。
- Tiny Transformer reward-model CPU smoke：训练进程仅接收 binary train 与 held-out-free readiness，在没有 combined 文件的目录实际执行 train-only tokenizer、完整 prompt+response、随机 GPT-2 scalar head、初始 \(\log2\)/tie、head+backbone update 与 lexical-counterfactual failure；在模型初始化前拒绝缺失/篡改 readiness 和顺序漂移 train。明确 authored pair、无密钥 artifact 与 CPU tiny overfit 不证明签发者、人类 preference、目标 RM、广泛 OOD、CUDA 或 reward hacking。
- 目标 RM LoRA/QLoRA 训练入口：固定 model revision、held-out-free train/readiness gate、独立 readiness-only 与 target-tokenizer-only preflight、超限 fail closed、scalar head 保存、可选 centering 与 trainer 后 pair-count 复核；本地随机 tiny GPT-2 实际执行正式 RewardTrainer、SEQ_CLS LoRA、optimizer step、metrics 与非零 `lora_B` adapter 保存，明确未验证目标 checkpoint/module names、CUDA/QLoRA、显存、收敛或质量。
- 严格 SFT 数据 reference core 与 CLI：版本化 JSONL 契约、对话状态校验、递归不可变 metadata、required split、重复 id/exact messages、group/content 跨 split 门禁、有序/无序/manifest canonical identity，以及明确的 near duplicate、许可、PII 与 tokenizer/mask 非覆盖边界。
- TRL LoRA 与 QLoRA 入口统一复用 train-only 数据门禁，只向 trainer 传递结构化 messages，并在模型下载/初始化前保存 `sft-data-audit.json`；新增 train-only、三 split fixture 和解析/CLI 回归测试。
- 将 TRL 依赖收紧到已验证的 0.29 API 系列，避免宽泛下界安装不支持当前 `SFTConfig.max_length` / `assistant_only_loss` 契约的旧版本。
- 单卡训练入口在模型权重加载前实际执行目标 tokenizer chat template，拒绝缺失/全零/错长/非二值 assistant mask 与静默右截断，保存逐样本 `sft-template-mask-audit.json`；报告明确不冒充独立 mask 语义或最终 collator-label 验证。
- LoRA/QLoRA 入口支持显式 `--chat-template-path`，让不含 generation 标记的 checkpoint 使用经审核、版本化且由 preflight/trainer 共享的本地模板。
- 新增完全离线的 TRL SFT smoke：随机 tiny GPT-2、本地 WordLevel tokenizer 与 generation-aware template 实际贯通 strict records、assistant mask、collator `-100` labels 和 optimizer step，并验证 tiny batch loss 下降；明确不冒充目标模型/CUDA/真实数据证据。
- 审计准备阶段强制接收 combined split artifact，并逐记录、顺序敏感地验证 train-only 输入正是其 train 子集；输出独立 train/split audit 与 canonical binding manifest，避免“审计 A、训练 B”。
- 新增透明的 SFT lexical near-duplicate reference：显式 NFC 或 NFKC/casefold/whitespace profile、full/user/assistant view、Unicode code-point n-gram set、精确 Jaccard 分子/分母、跨 split 比较分母与 manifest；CLI/训练 readiness fail closed，同时明确 candidate 非语义重复、阈值未校准且全对实现不适合大规模。
- SFT `prepare-training` 两阶段权限流水线：独立审计进程读取 train + combined，输出严格可重载且不含 held-out 原文的 readiness；TRL/QLoRA trainer 只读取 train + readiness，并在模型加载前拒绝重复/未知字段、错误版本、失败 gate、篡改和陈旧绑定。新增 held-out 文件移除、artifact tamper 与 train mismatch 回归，并明确无密钥 hash 不是来源认证。
- SFT governance gate 与 readiness v3：严格 source/license registry 默认拒绝未知组合，并绑定 purpose、evidence、review time、expiry 和 risk label；有限 email/key/token/JWT/private-key/Luhn 候选扫描不在报告写入命中原文，人工 exception 绑定精确 record/span 且陈旧例外失败。v3 进一步把 tool call/schema surface 纳入 identity、lexical 与 sensitive-candidate audit。CLI、训练 gate、fixture 与测试明确 registry 非法律意见、扫描未命中不证明无 PII/secret。
- 严格 pairwise preference 数据契约与 CLI：保留 A/B presentation order、`a/b/tie/invalid`、strength、rubric、annotator/adjudication、generator revision 和治理/split 字段；检测交换候选后的 duplicate pair、exact prompt/pair/group 跨 split 泄漏，并只允许二元 train pair 转 DPO row。
- 完全离线 TRL DPO smoke：本地 WordLevel tokenizer、随机 tiny GPT-2 与冻结 reference 实际贯通 preference record、chat template、chosen/rejected collator、completion mask 和 optimizer；验证初始 loss≈`log(2)`、tiny-pair loss 下降、reference 参数不变，且不冒充人类偏好、目标模型、CUDA 或安全对齐证据。
- Preference 生产训练边界：新增独立 binary train artifact、有序 train/combined binding、严格 held-out-free readiness v2 与篡改/陈旧/失败 gate 拒绝；tie/invalid 留在 combined audit 而不强制成 winner。新增 prompt↔prompt 和跨 A/B candidate 的透明 lexical gate、prompt/两侧 candidate 的 source/sensitive governance、目标 tokenizer prefix/空 completion/同 token/截断 gate，以及 LoRA/QLoRA TRL DPO 入口；明确 registry/regex/字符阈值不是法律、无敏感信息或语义无污染证明。该通用 trainer/QLoRA 路径只在 tiny CPU fixture 上验证；仓库另有固定 Qwen CPU FP32 DPO 单步 control，仍没有 CUDA/QLoRA 证据。
- 原始偏好判断与评测协议：新增严格 raw judgment artifact、validation/test case 绑定、固定 rater 数、双 presentation-order 覆盖、重复 annotator-case/rubric/盲化与独立声明 gate；可复算 raw pairwise agreement、等 rater Fleiss’ κ、逐 case A-position effect 和 case-cluster percentile bootstrap。示例为 authored fixture，不证明标注者是人、真实盲化/随机分配、rubric 有效或位置效应具有因果性。
- 推理压测计时与到达边界：attempt artifact 保存全量 scheduled `offered_at` 与 HTTP dispatch `started_at`，报告 client queue、成功请求 offered TTFT 和全 attempt offered-to-terminal percentile；负载发生器新增 burst、constant 与 seeded-Poisson finite schedule，open-loop 到达不等待 completion。机器输出同时保留 nominal/realized rate、offsets、seed，并明确 scheduled timestamp/有限 coroutine 不证明 event loop 跟得上、server queue 或 GPU 容量。
- RAG 目标 tokenizer packing：新增 `pack-tokenized`，用指定 tokenizer revision 与 checkpoint/override chat template 对每个 prospective 完整 prompt 重计数，显式预留输出，保存模板/prompt SHA-256、最终 token IDs 和逐候选决策；原始模板槽位替换不会递归解释 query/context 中的占位符文本。明确 tokenizer/revision/hash 不认证与部署权重或真实 context window 匹配，本地 WordLevel 测试不证明生成忠实度或目标模型能力。
- RAG generation trace binding：新增严格版本化 trace 与 `audit-traces` gate，对 case/answer/trace exact join，绑定 query hash、tenant/principals、按序 chunk source/version/content、canonical context、prompt/raw-output/answer identity，并覆盖多 chunk 同源与篡改测试。authored fixture 不证明真实模型执行、目标 tokenizer、历史 corpus、raw-output→claim 语义一致性、判断正确性或 unsigned artifact provenance。
- RAG end-to-end extractive baseline：新增 `answer-extractive` / `evaluate-extractive`，串联 authorization-first BM25、byte-budget packing、exact character span、短引用、lexical coverage abstain、统一 RecordedAnswer 与独立 fingerprint artifact；测试覆盖 ACL/tenant、budget drop、跨来源、topic-related no-answer、offset/source tamper、确定性及 qrel 隔离。5 条 authored fixture 的 3-answer/2-abstain 回归不证明语义相关、来源真实、答案完整、阈值校准、模型执行或 token-window 合规。
- Evaluation run manifest：统一评测 JSON/JSONL 改为拒绝 duplicate key、未知字段和 `NaN/Infinity`；`score` 输出严格版本化 manifest，绑定 ordered case 全语义、recorded answers、scored results、metric revisions、scorer 与 caller-supplied system id，`compare` 在 bootstrap 前重算并拒绝同 ID 换 gold、结果篡改或 metric revision drift。authored fixture 与无密钥 hash 不认证真实系统执行、case/metric 有效性或线上影响。
- Evaluation comparison artifact：`compare` 输出升级为严格版本化、可重载 artifact，将 bootstrap 参数、质量/安全/延迟与 protected-slice 阈值、两侧 run manifest、metric revision、统计结果和失败原因纳入 canonical fingerprint，并新增均值差/gate 内部一致性、threshold/schema/fingerprint 篡改测试。`verify-comparison` 机器输出明确声明只校验当前 artifact，不重开引用输入或重算 bootstrap；无密钥 fingerprint 不防整套协同重写或历史截断。

### Changed

- 将学习目标从通用科普调整为研究生教材、工程落地和求职准备。
- 将 RAG、Agent、微调、推理部署和评测设为重点实践方向。
- 明确区分 Gemini Interactions API 与 `generateContent`，并按 TRL 实际 generation/assistant mask 规则修订单卡 SFT 指引。
- 将六个模型家族章节重构为“机制/契约—证据边界—可运行实验—工程选型—面试追问”的教材结构。
- 知识地图与工程项目索引改用证据驱动的真实成熟度，不再把“有导航页”视为完成。
- code CI 同时覆盖 Python 3.10/3.12，并约束 NumPy 版本以兼容 Python 3.10 mypy target。
- 将预训练与分布式训练从概述扩成研究生教材级章节，补数据混合、FLOPs/通信模型、数值稳定、精确恢复和正确性验收。
- 重写数学基础为 LLM 直觉主线，补 shape/broadcast、attention 推导、KL、VJP、AdamW、浮点误差和可信统计实验。
- 重写机器学习与 NLP 基础，补统计风险、代理目标、全局 token 聚合、分布偏移、语言模型概率、困惑度边界、解码、语料工程和分层错误分析。
- 为基础章节增加不可删除的准确性边界检查，保护 DDP token mean、Tokenizer 契约、跨 tokenizer PPL 和结构化输出等关键限定。
- 将生成与解码从策略概述扩成完整协议，补 top-p 边界、processor 顺序、beam/停止/流式/结构化输出与复现实验；MiniGPT 新增经过测试的 top-p 采样。
- 重写规模化规律，补 `6ND` 口径、power-law 解析最优点、拟合不确定性、MoE/长上下文与生命周期成本；新增可执行 scaling 计算器及解析测试。
- 重写架构谱系与机制可解释性，补 mask/SSM/MoE 比较、证据阶梯、activation/path patching、SAE、模型编辑和目标模型实证边界。
- 重写训练数据工程，补 source registry、不可变快照、解析验收、去重/污染、PII、混合、packing、删除 lineage 与发布不变量。
- 重写偏好对齐与持续学习，补 RM/PPO/DPO 目标、偏好数据偏差、DAPT/replay/蒸馏、合并与 unlearning 威胁模型；新增稳定的 Bradley–Terry/DPO 数学实现和 14 个测试。
- 重写系统安全与治理，补注入攻击链、RAG/Agent/SSRF/secret/供应链控制、隐私与公平、影响评估、第三方、事件和证据边界。
- 重写硬件与端侧部署，补容量账本、Roofline、prefill/decode、量化/kernel、单卡/端侧测量；新增可执行 Roofline 下界和 12 个测试。
- 重写多模态基础，补视觉架构、动态分辨率、OCR/图表、ASR/TTS、视频、grounding、评测与跨模态安全；新增 CER、box/temporal IoU 和 12 个测试。
- 重写推理增强/长上下文/MoE 与具身/GUI/小模型章节，补 test-time scaling、有效上下文矩阵、router/通信、闭环控制、sim-to-real、路由、speculative 与联邦边界。
- 重写 Prompt Engineering，补真实 chat template、任务/输出契约、few-shot 选择、工具边界、Prompt injection、评测和可重放 artifact graph。
- 重写代码模型、对话记忆与 LLMOps，补仓库级定位、执行沙箱、pass@k、typed state、摘要损失、trace、canonical identity、发布与漂移协议。
- 强化系统设计、面试题与简历项目，补需求量化、容量公式、故障树、降级不变量、DPO/MoE/长上下文、安全追问和逐数字证据账本。
- 将对话状态从纯协议扩展为可执行 reference core，同时明确它不是持久化、分布式或生产授权服务。
- 扩写评测方法中的概率事件、分箱约定、校准证据和 coverage-risk 取舍，明确模型自述 confidence 不等于任务正确概率。
- 将合成数据从数据工程短节扩展成独立研究生章节，并明确 verifier pass、synthetic token 数和平均分都不能单独证明质量或避免 collapse。
- 修正 SSE benchmark 的 failure accounting：预期 429/5xx/timeout/protocol error 成为一等 attempt，不再因单请求失败丢失整轮统计；未知代码错误仍阻断基准。

## [0.1.0] - 2026-08-05

### Added

- LLM 基础、训练、推理、应用、质量、安全与前沿的首版中文教材。
- MkDocs Material 站点、搜索、MathJax、Mermaid 和严格构建检查。
